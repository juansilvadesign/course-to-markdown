# Course → Markdown — Build Task Tracker

> **Open this first, every session.** This file is the running execution state. Check a box only after the artifact and its verification exist. Deferred/v2+ work lives in [`ROADMAP.md`](ROADMAP.md); decisions and dated operational state live in [`MEMORY.md`](MEMORY.md); commands and architecture live in [`README.md`](README.md).

**Two levels of done — do not conflate them:**

- **Pipeline MVP — ✅ SHIPPED.** Owned media can move through Stage 0 download, Stage 1 transcription, and Stage 2 staged knowledge-pack compilation. The full Motion Boost and JStack Full Stack courses proved the path end to end.
- **Authenticated-platform expansion — the current release.** DesignBoost and the supplied Skool classroom URL have working, session-backed adapters; the remaining JStack catalog is not complete until every accessible lesson is downloaded, transcribed, and count-reconciled.

## Delivery frame

- **Timebox:** the current implementation cycle, closing at a verified handoff rather than a calendar date.
- **Capacity:** one local workstation; at most one long media download and four non-overlapping, isolated transcription batches at the same time (fan-out defaults to three); existing Gemini paid tier and user-exported sessions.
- **Fixed outcome:** reliable DesignBoost + Skool support and a finished JStack Stage 0/1 corpus.
- **Open scope:** convenience features, attachment capture, bulk Skool discovery, and Stage 2 compilation may move to [`ROADMAP.md`](ROADMAP.md) to protect the fixed outcome.
- **Replan trigger:** a platform response shape changes, a session expires, DRM is encountered, or provider quota prevents a clean resumable run.

## ✅ Checkpoint — 2026-08-04: JStack Stage 0/1 COMPLETE

**The whole JStack corpus is transcribed and reconciled: 1,668/1,668 lessons.**

| batch | transcripts / media |
|---|---|
| `jstack-lives` | **1084 / 1084** — 69/69 courses, `reconcile.py` reports 0 needing attention |
| `jstack-projects` | 182 / 182 |
| `formacao-full-stack` | 372 / 372 |
| `formacao-typescript` | 30 / 30 |

Zero `.part`, zero `.ytdl`, zero zero-byte files anywhere in `input/`/`output/`.

- **The Gemini pause was LIFTED on 2026-08-04, with explicit user approval, for a bounded 9-lesson sweep** — see Phase 3.5. It is no longer a standing block, but the default provider remains `openrouter`; do not read this as a general licence to run Gemini batches.
- **Provider provenance is now mixed — record it, it matters for Stage 2.** The corpus is MiMo/OpenRouter except: 1 Fincheck lesson (see 3.3) and the **9 lives lessons** listed in 3.5. If a Stage-2 pack ever shows a fidelity discrepancy, check provenance before blaming the compiler.
- **46 numbering gaps remain advisory across the lives tree.** Only a live catalog session can settle whether those are real lessons or JStack's own skipped numbering; `reconcile.py` flags but never fails on them.

### Superseded (kept for the reasoning, not the instruction)

The 2026-07-28 checkpoint paused Gemini and listed a three-minute Fincheck repair command as the first action after resume. Both are dead: Fincheck was repaired via OpenRouter/MiMo on 2026-08-04 (3.3), and the pause was lifted for the sweep above.
- **Quality guards reject three failure modes:** a non-`STOP` finish reason, an exact 8-word sequence repeated 50+ times, and a words-per-minute floor that catches the silent audio-drop. The offline suite passes **26/26**.

---

## ✅ Done so far

- [x] **Stage 1 is production-proven.** Nested input mirroring, UTF-8 paths, ASCII-safe Gemini uploads, long-audio chunking, per-request timeout, quota-aware clean stop, and resumable output all survived full courses.
- [x] **Stage 2 is production-proven.** Packs are compiled on the Claude subscription, staged for review, and never auto-promoted into `knowledge/`.
- [x] **JStack downloader generalized.** `downloaders/jstack.py` covers lives, projects, and trainings; cursor pagination, per-library Bunny hosts, YouTube outliers, audio-only default, manifests, and `--resume` are implemented.
- [x] **New JStack cookie export verified (2026-07-28).** `input/cookies-jstack.txt` authenticates and sees the full 74-item catalog: 69 lives, 3 projects, and 2 trainings.
- [x] **JStack trainings complete.** `formacao-typescript` and `formacao-full-stack` are downloaded and transcribed; Full Stack has also been compiled and promoted through the human-review flow.
- [x] **JStack projects downloaded.** Fincheck, Foodiary, and WaiterApp media trees are complete and manifest-backed.
- [x] **DesignBoost adapter implemented.** `downloaders/designboost.py` authenticates through the exported member session, lists all four catalogs, resolves course/workshop module trees plus lives and AI-course entries, and delegates Vimeo media to the shared downloader core.
- [x] **DesignBoost live dry-run passed (2026-07-28).** Course `32` resolved 12/12 lessons across two modules, with zero unresolved videos.
- [x] **Skool adapter implemented.** `downloaders/skool.py` reads authenticated `__NEXT_DATA__`, preserves sets/sections, resolves external or signed native HLS video, extracts TipTap descriptions/resource links, and rejects commercial HLS DRM markers.
- [x] **Supplied Skool course live dry-run passed (2026-07-28).** `Equação de Valor` resolved 1/1 lesson and its signed stream with zero DRM/unresolved assets.
- [x] **Shared safety core added.** `_shared.py` owns Netscape-cookie parsing, query/token redaction, quiet signed-URL yt-dlp calls, DRM checks, atomic manifests, audio/video modes, and resumability.
- [x] **Bounded Stage 1 fan-out added.** `scripts/transcribe_batches.py` maps each direct child course to its own output root, caps concurrent subprocesses, validates manifests/partials, logs per course, and holds a batch lock so workers cannot overlap.
- [x] **Offline safety/contract tests added.** Thirteen unit tests cover cookie parsing, secret redaction, DRM discrimination, truncated/repetitive-model-output rejection, DesignBoost curriculum mapping, Skool Next-data parsing, nested curriculum flattening, native signed URLs/resources, TipTap text, and fan-out discovery/validation.

## ▶ Next session — start here

**Stage 0/1 is finished, committed, and pushed. The forbidden-path incident is CLOSED: 5.2, 5.3 and 5.4 are all done, and the 19 recompiled courses (29 pack files) are PROMOTED into `knowledge/coding/courses/` as of 2026-08-23.** In order:

1. ✅ **COMMITTED 2026-08-23c.** Parent at `8fac90f` (clean, pushed); submodule at `92ca712` (clean, pushed). The tarball gitignore question was decided and landed with it. `output/_review-5.4/` is durable, mirrored into `knowledge/backups/course-to-markdown/`.
2. ✅ **QUOTE CONTRACT — AMENDED 2026-08-24.** `SKILL.md` now carries a **spoken-source clause**: a lecture transcript is not printed prose, so **disfluency-only** cleanup is allowed when the entry is marked `(cleaned)` — a removed stutter, a fixed misspeak, an agreement fix. This legitimizes the 14 reworded quotes (all hand-verified meaning-preserving). **The location reference stays mandatory and is now an explicit FAIL in `verifier-agent.md`**, because a quote you cannot locate is unfalsifiable — so the **23 uncited quotes are the remaining debt to fix**, not the rewordings. Also written into the contract: the §5.6 synthesized-quote failure (correct content, invented wording, real citation) with the instruction *never treat "the cited lesson covers this topic" as evidence the quote is real*.
3. ✅ **TOKEN CAP — RAISED AND, MORE IMPORTANTLY, UNIT-CORRECTED 2026-08-24.**
    - ⛔🔴 **The old evidence was stated in units the gate never computed.** `verifier-agent.md` gated on `wc -w × 1.33`; `pack_fidelity.py`/`pack_coverage.py` measured `chars / 3.7`. On the same 91 library course packs the two disagreed by a **median 1.42×** — 27 vs 73 packs over the old 2k cap. The recorded "25 of 29 exceed, median ~2.9k, max 4,344" was the **chars/3.7** reading (`paginacao-…-b.md` = 4,344 confirms it), so "over cap" was unreadable without naming the instrument. ⭐ Same family as *print the value the gate gated on*.
    - **Resolution: one method everywhere (`chars / 3.7`, PT-tuned — `words × 1.33` is an English heuristic that understates a PT-BR body by ~1.4×), and every type re-baselined to its measured p90** so the switch corrects the unit instead of silently tightening: **article 1,000→1,350 · guide 1,500→2,000 · book 2,000→3,000 · course 2,000→3,500.**
    - **Files changed (8 touch points, 6 files):** `SKILL.md` (global rule + skeleton + new quote hard-rule), `article.md`, `guide.md`, `book.md`, `course.md`, `agent.md` ×2, `verifier-agent.md` ×2, plus `TOKEN_CAP 2000→3500` in both `pack_coverage.py` and `pack_fidelity.py`. ⚠️ All four playbooks carried their own cap — a `course.md`-only edit would have left three stale.
    - ✅ **Verified by known-bad rerun, per the standing rule that a loosened gate must be re-proven:** `pack_coverage.py` over the v1 corpus still returns **18/69 pass**, with invented-date (`2024-07-30`), truncation (1286B) and coverage (0/13) detection all still firing. **The cap is not inert** — library-wide FAILs go 91/195 → **8/195**, and those 8 are genuine outliers (worst: `sales/articles/oultimomentor/101_agencia.md` at 9,117 vs a 2,000 guide cap; `intensivo-cliente-pago.md` at 8,391 vs 3,500).
    - ⚠️ **Not ground truth.** No tokenizer is installed here; `3.7` is a rule of thumb, not a measured tokenization. If a real count ever matters, install one and re-baseline — the caps are p90s of an estimate.
    - ⛔ Don't force three-way splits to satisfy the number. 73 of 91 exceeding the old cap was the corpus disagreeing with the cap, not 73 compilers misjudging.
4. **Decide the 3 term-loss lessons** (5.5) — re-transcribe on Gemini, or accept the omission and record provenance.
5. **The other 50 — ✅ GATED 2026-08-23b, 8 promoted, 42 still held. See 5.6.** The provenance claim above holds (checked independently: they carry `compiled: 2026-08-05`, not the forbidden script's invented `2024-07-30`) — but **"legitimate provenance" is not "promotable"**, and the gate proved it twice over. Next: decide what to do with the 32 that fail coverage and the 9 that fail quote provenance.
6. **Answer the two open acquisition-scope questions** below before touching DesignBoost or Skool.
7. ✅ **RETENTION — DECIDED AND EXECUTED 2026-08-23b, the `TASKS.md` open question below is closed.** Source media deleted: **22.18 GB → 0.69 GB (21.49 GB reclaimed, 1,615 files)**. Kept: the 55 media files of the **3 courses phase 5.5 needs** (`gerenciamento-de-estados-com-zustand`, `dominando-o-react-hook-form`, `websockets-e-cronjobs-serverless-na-aws`).
    - ⛔🔴 **`input/` was NEVER only media, and an `rm -rf` of the course dirs would have destroyed the ordering authority.** It also holds **74 `manifest.json`** — which §3.5/§5.2 both name as the only thing that establishes curriculum order, because 6 modules across 3 courses name *every* lesson `1000-<title>` so filename sort silently goes alphabetical — and **1,705 `.description.md`** sidecars carrying `**Autor(es):** Mateus Silva` (without which agents fall back to `author: JStack`). All 1.85 MB of it was **kept in place and additionally mirrored** to `knowledge/backups/course-to-markdown/input-metadata/`. Deletion was by **media extension**, never by directory.
    - ⛔🔴 **Transcripts were never in git and never will be** — `.gitignore:13` `/output/*`, by the hard rule "transcripts never enter Git", because **this repo is public**. Before this session the 1,688 transcripts existed in exactly **one** place on one disk. They are now mirrored (browsable tree + tarball + `INDEX.md`) into the **private** parent repo at `knowledge/backups/course-to-markdown/`. ⭐ The deletion was gated on asserting the transcript count agreed across **three** independent locations first.

The jargon audit that used to head this list has now been **run** — result and remaining decision in 5.5.

Standing rules that outlived the checkpoint: media and transcripts are the source of truth, never terminal history; never launch an aggregate worker over live per-course workers; always pair `-o` roots explicitly. **New (2026-08-05):** a pack's *existence* proves a file was written — never that the course was read.

---

## Phase 1 — Reliable local pipeline ✅ DONE

- [x] **1.1 Stage 1 CLI** — recursive media discovery, folder mirroring, Gemini/Groq provider seam, language hint, dry-run, overwrite, and retranscribe modes.
- [x] **1.2 Audio hardening** — ffmpeg normalization, UTF-8 re-exec, ASCII upload names, long-file chunking, and temporary-file cleanup.
- [x] **1.3 Provider resilience** — retry/backoff, request timeout, daily/hourly quota detection, clean resumable stop.
- [x] **1.4 Stage 2 handoff** — transcript-first output and subscription-backed `course-module-compiler`.
- [x] **1.5 Human promotion gate** — packs remain staged until reviewed.

## Phase 2 — Authenticated platform adapters

- [x] **2.1 JStack** — trainings, projects, and lives; full catalog enumeration and resumable batches.
- [x] **2.2 DesignBoost reconnaissance** — member app/API boundary, bearer-session cookie, four catalog types, module/class detail shapes, Vimeo media.
- [x] **2.3 DesignBoost implementation** — list, single content, catalog batch, limit, dry-run, resume, audio/video modes.
- [x] **2.4 DesignBoost verification** — authenticated 12-lesson dry-run and offline parser tests.
- [x] **2.5 Skool reconnaissance** — authenticated Next data, one-module target curriculum, native signed HLS shape, normal browser user-agent requirement.
- [x] **2.6 Skool implementation** — course URL, curriculum/description/resource parsing, native/external video resolution, DRM guard, resume.
- [x] **2.7 Skool verification** — authenticated target-course dry-run and offline parser tests.
- [x] **2.8 Documentation pass** — README, operating contracts, memory, task tracker, and roadmap agree with the shipped behavior.

## Phase 3 — Finish the JStack corpus

- [x] **3.1 Trainings media + transcripts** — both formations complete.
- [x] **3.2 Projects media** — Fincheck, Foodiary, and WaiterApp complete.
- [x] **3.3 Projects transcripts — ✅ DONE 2026-08-04.** 182 media / 182 transcripts, quality scan **0 flags (CLEAN)**: no repetition loops, no empty/orphan/missing transcripts, no `.part` leftovers, no zero-byte media. The Fincheck lesson `02-front-end/03-dashboard/04-implementando-dropdown-de-sair-da-conta` was repaired **with MiMo-V2.5 via OpenRouter, not Gemini** (Stage 1 stays paused): 11,928 words / 8-gram ×1,051 → **10,444 words / ×4**, diacritics intact. ⚠️ This is the corpus's **only non-Gemini transcript**. Broken original backed up in the session scratchpad; scan script rebuilt at `scratchpad/quality_scan.py` (the `/tmp` original was cleared).
- [x] **3.4 Lives media — ✅ DONE 2026-08-04.** 69/69 courses, 1,084 media, ~13 GB, after the cookie was renewed. Zero local courses absent from the catalog.
- [x] **3.5 Lives transcripts — ✅ DONE 2026-08-04. 1084/1084.** Ran as three passes of `scripts/transcribe_batches.py --jobs 3`: `openrouter` took 542 → 1069 (**$1.2511 for 527 lessons = $0.0024/lesson**, beating the $0.0032 baseline — the Xiaomi pin holding); a second `openrouter` pass recovered 5 more at **$0.022/lesson**, 9× the clean rate, because a looping generation bills to the `max_tokens` ceiling. The last 10 split into two unrelated causes:
    - **1 was a pipeline bug, not the model** — see the `MIN_DURATION_FOR_WPM_SEC` fix under 4.4. Recovered on `openrouter` once fixed (`dynamodb-single-table…/10-paginação-no-dynamodb`, 5,275 w @ 195 wpm).
    - **9 looped deterministically on MiMo**, failing all 4 retries on two consecutive passes. ⛔ **Retrying is the documented cure for the *stochastic* loop but does nothing for these** — the cure is a provider switch. Transcribed with `--provider gemini` (pause lifted with explicit user approval), sweep returned **69/69 clean, exit 0**. These 9 are the corpus's Gemini island:
      | course | lesson |
      |---|---|
      | `dominando-o-react-hook-form` | `11-limpando-erros-com-clearerrors…` |
      | `autenticacao-jwt-em-apis-node-js` | `03-entendendo-o-fluxo-de-autenticação-com-sessions` |
      | `gerenciamento-de-estados-com-zustand` | `02-ferramentas-de-gerenciamento-de-estado` |
      | `processamento-de-imagens-assincrona-…` | `05-redimensionando-a-imagem-com-o-sharp` |
      | `s3-mais-seguranca-com-presigned-posts` | `07-criando-conditions-no-presigned-post` |
      | `otimizando-configurando-ci-cd-…` | `02-conhecendo-os-presets-do-tailwindcss` · `03-criando-o-access-token-no-npm` |
      | `upload-para-o-s3-com-lambda-functions-…` | `03-configurando-as-permissões-da-lambda-no-iam` |
      | `sql-subqueries-transactions-e-triggers` | `04-trabalhando-com-as-transactions-na-prática…` |
- [x] **3.6 Reconciliation — ✅ DONE 2026-08-04.** `scripts/reconcile.py input/jstack-lives output/jstack-lives` → **1084/1084 across 69 courses, 0 needing attention.** 46 numbering gaps remain advisory (only a live catalog session can settle them).
- [x] **3.7 Failure sweep — ✅ DONE 2026-08-04.** No `failed` manifest entries, no `.part`/`.ytdl`, no zero-byte media or transcripts, no audio file without a non-empty transcript — across all four batches.

## Phase 4 — Release verification and handoff

- [x] **4.1 Import/compile smoke** — every downloader imports and both new CLIs expose help successfully.
- [x] **4.2 Offline safety/contract suite** — `.venv/bin/python -m unittest discover -s tests -v` passes 13/13.
- [x] **4.3 Authenticated adapter smoke** — DesignBoost and Skool dry-runs pass without downloading media.
- [x] **4.4 Full test rerun — ✅ 26/26 passing (2026-08-04).** Grew from 13 with two regression tests for the wpm-floor fix: `MIN_DURATION_FOR_WPM_SEC` (30s) exempts short trailing chunks, because `ffmpeg -f segment` leaves a sliver whenever a lesson runs just past a multiple of the chunk length (1622s at 540s → a 2.0s tail; the one word genuinely spoken there scored 28.2 wpm and failed the whole 27-minute lesson). The second test asserts the exemption is not a hole — a real silent-drop on a 60s chunk still fails.
- [x] **4.5 Git hygiene — ✅ verified 2026-08-04.** No cookies, API payloads, media, transcripts, tokens, or signed URLs tracked or untracked-visible; `input/`/`output/` stay ignored. Code landed in `54dfafa` (backend pin + short-chunk density exemption + 2 tests); the docs above are the only uncommitted files.
- [x] **4.6 Durable state — ✅ 2026-08-04.** Final counts, provider provenance, and the cost-per-lesson figures are recorded in this file and `MEMORY.md`.

## Phase 5 — Stage 2 for the new corpus

> Not required to finish the current Stage 0/1 request. Start only after Phase 3 count reconciliation.

- [x] **5.1 Choose reusable-unit granularity — ✅ DONE 2026-08-05.** Course/project/discipline granularity for JStack projects (6 packs) and JStack lives (74 packs across 69 courses). **This decision stands** — only the execution below was defective.
- [x] **5.2 Compile in batches of no more than four agents — ✅ CLOSED 2026-08-21.** All 19 recompiled through `course-module-compiler` on the Claude subscription; `scripts/compile_lives_stage2.py` deleted (`git rm`, commit `b48f097`). Coverage 22–66% → **100% on every course**. Original defect record below, kept as the reason the rerun happened:

    - 🔴 **(historical)** 102 `.pack.md` files exist in staging, but **19 of them were written by `scripts/compile_lives_stage2.py`, which calls `gemini-2.5-flash` over the Gemini API** — the one thing Stage 2 must never do. The earlier "subagents and batch script" wording hid that the batch-script half *is* the forbidden path. Those 19 must be recompiled through `course-module-compiler`. Its three defects, all in its own config:
    - ⛔ **`[:60000]` prompt slice (plus a 15k-per-lesson cap) — the compiler never read most of the course.** All 19 courses overflowed the window: coverage **22–66%, median 37%**, across **272 lessons**. Worst: `login-social-federated-com-aws-cognito`, 25 lessons → **22%**. This is the defect that matters most, because nothing downstream announces it.
    - ⛔ **`max_output_tokens=3000` with thinking left ON** — the budget goes to reasoning, so **6 packs are truncated mid-sentence** (358–725 B against an 8.9 KB median; 5 of the 6 are in this cohort). The hard rules already say "thinking off", and the pipeline's own `transcribe.py` honours it; this script did not.
    - ⛔ **`import datetime` present but unused → the model invented the date.** All 19 carry `compiled: 2024-07-30`, two years in the past — the same hallucination the 2026-06 fix cured on the transcription side.
    - Across all 102 packs: **28 lack the required `## How to apply`** (14 in this cohort, 14 dated today) and 3 carry quoted-date drift (`compiled: "2026-08-05"`).
- [x] **5.3 Reconcile lesson coverage — ✅ CLOSED 2026-08-21 by `scripts/pack_coverage.py`** (content-based, 19/19 pass, 272/272 lessons). Historical note: The previous "100% of 1,668 transcripts, zero orphans" was a **counting artifact**: it asserted that every transcript sits under a course that *has* a pack, never that its content reached the compiler. Under the 60k slice it demonstrably did not. Re-reconciliation must be **content-based** — assert what the compiler actually ingested, not folder membership.
- [x] **5.4 Review, then promote — ✅ DONE 2026-08-23. All 19 reviewed against the TRANSCRIPTS, 29 pack files promoted into `knowledge/coding/courses/`.**
    - **v1 was NOT the authority, and could not be.** It read a median 37% of each course and **7 of the 19 v1 packs are 358–1,286 B truncated stubs** that die inside the TL;DR. Where v2 agrees with v1 that agreement covers only the slice v1 saw — it corroborates the partiality, not the pack. Fidelity was judged against the transcripts; v1 was kept only as a regression diff (`--v1-diff`), which found nothing lost.
    - **New instrument: `scripts/pack_fidelity.py`.** Gate Q (every `## Quotes worth keeping` entry must trace to a transcript, separating fabricated / misattributed / reworded), Gate S (subject-term dropout — the substitution class a v1↔v2 diff structurally cannot see), Gate T (token measurement, recorded not graded).
    - ✅ **Zero fabrications across all 19 courses** (53 quotes). ✅ **Zero transcript corruptions propagated into any pack.**
    - 🟡 **Quote-contract debt, deliberately promoted as-is:** `SKILL.md` requires quotes be *"Verbatim, with location reference."* Actual: **13 VERBATIM · 14 REWORDED · 23 UNCITED · 1 STITCHED · 1 INCONCLUSIVE.** The 14 rewordings are all **spoken-disfluency cleanup** (a fixed misspeak, a pluralised verb, a removed stutter) — meaning preserved in every case, hand-verified. Fix later or amend the contract; it is not wrong knowledge.
    - 🔴 **The compiler SILENTLY REPAIRS corrupted transcripts.** `Svelte ×6`→`Zustand`, `Zustend ×16`→`Zustand`, `form setval`→`form.setValue` (this one it even annotated). Good for the packs, but it means transcript corruption is **invisible downstream** — the transcript is the only place it can be caught. This is why Gate S reads transcripts, not packs.
    - **`tokens_estimate` was wrong on 25 of 29 packs** — 17 under-stated, 8 over-stated, 4 within ±50 (worst +979). Corrected from measured character count during promotion. ⛔ The "agents guess low" rule is a *tendency*, not a law.
    - ⛔ **Scope: only the 19.** The other 50 jstack-lives courses keep their v1-era packs in staging and were NOT promoted. Three forward references in the promoted packs point at them and dangle on purpose.
- [ ] **5.5 Jargon audit — ✅ RUN 2026-08-05; remediation still open.** `--reference` against a purpose-built 19-lesson Gemini baseline (`output/jstack-lives-gemini-ref/`, swept 00:16): **3 of 19 pairs show real term loss.** One is the exact documented failure — `gerenciamento-de-estados-com-zustand/1000-conteúdo/06-configurando-o-zustand-com-o-typescript`, where Gemini has *"ele tá como **unknown**, porque eu não passei nenhum generic"* and MiMo has *"ele tá como **any**"* (`unknown` ×8 → ×0). The other two lose `callback` ×7 and `context` ×3.
    - ✅ **It did not reach the library.** That pack propagates no wrong `any`/`unknown` rule — neither term survived compression, so this is an **omission, not an inversion**. Still decide whether the 3 lessons get re-transcribed on `--provider gemini` before their packs are rebuilt.
    - ⛔ **Bare mode confirmed useless at scale:** 252 of 1,084 flagged (23.2%) — the documented base-rate noise. Only `--reference` discriminates.
    - ⚠️ **Tool wart:** the heuristics run *even in* `--reference` mode (`jargon_audit.py:138-139`), so a tree-vs-tree invocation buries the 19 real diffs under 252 noise flags and looks like it found nothing. Filter on `TERM LOST`/`down N%`, or make `--reference` suppress the heuristics.
- [ ] **5.6 The other 50 — gated 2026-08-23b. 8 promoted, 42 held.** Two gates run in sequence, each halving the cohort.
    - **Gate 1, coverage (`pack_coverage.py`, never previously run on these): 18 pass / 32 FAIL, median 58%.** That is the *same failure class* as the forbidden cohort's median 37%, only milder — lessons absent from the `## Curriculum` spine. ⛔ So the standing note that these packs are "legitimate" was true only about **provenance** (verified independently: they carry `compiled: 2026-08-05`, not the invented `2024-07-30`); it was never a statement about content, and it read like one. Worst: `pipeline-de-ci-cd-…-codebuild` at **2/13 (15%)** — no `## Curriculum`, 725 B, TL;DR dies mid-sentence on *"para garantir a"*, `tokens_estimate: ~1800` vs ~192 actual. That is the **6th** truncated pack; §5.2 recorded "5 of the 6 are in this cohort", so it was predicted and is now located.
    - **Gate 2, quote provenance, on the 18 survivors: 9 pass / 9 FAIL.** One of the 9 (`rbac-criando-um-sistema-…`) was **already in the library** under the odd-one-out name `jstack-live-rbac-controle-de-acesso.md`, byte-identical, so **8 courses / 12 pack files** were actually written. Library **60 → 72**.
    - ⭐🔴 **Gate Q produced false-positive round #5 AND a true positive in the same run; only re-measuring separated them.** It reported **38 NOT_FOUND across 82 quotes**. Hand-checking `conhecendo-o-framer` found 4/5 and 3/5 distinctive fragments of two "fabricated" quotes present **in exactly the lesson they cite** — real quotes *stitched from non-contiguous spans*, which Gate Q's single-window similarity structurally cannot see. ⛔ Never read a NOT_FOUND count as a fabrication count.
    - ⭐ **But the alarming reading was RIGHT for a subset, and the discriminator is per-course clustering.** New instrument `evidence/quote_ngram.py` scores a quote's 4-gram coverage against the whole course. **Validated against planted fabrications before being trusted**: two invented quotes written with topical vocabulary scored **0.0%**, while the two real flagged quotes scored 68% and 46%. Scores then clustered **by course, not randomly** — `dominando-o-serverless-framework` 79–100% on all 6, `dynamodb-e-serverless-framework-o-par-perfeito` **0–21% on all five**, one at literally 0%.
    - 🔴 **The real defect is SYNTHESIZED QUOTES: correct content, invented wording, presented in quotation marks with an attribution.** The dynamodb pack's concepts are genuinely taught (partition key ×5, hash ×6, `DocumentClient` ×1) and its cited lessons all exist — but the instructor says *"Muito bem. E aí a gente tem agora uma questão aqui dentro do Dynamo. E é muito massa, tá?"* while the pack quotes polished written prose. **The promoted 19 had zero of these.** ⭐ A pack can be topically accurate, cite real lessons, and still be quoting nobody.
    - **Held back (9):** 🔴 synthesized — `dynamodb-e-serverless-framework-o-par-perfeito` · `construindo-e-publicando-ui-kits-…` · `dominando-o-prisma-orm` · `dynamodb-streams-e-search-com-algolia` · `dominando-o-tailwind-css`. 🟡 mixed — `construindo-um-gerenciador-de-estados-do-zero-com-react` · `documentando-componentes-com-storybook` · `dominando-os-react-hooks` · `deploy-de-apps-react-para-producao-na-aws`.
    - ⛔ **Two instrument bugs of my own, each of which fabricated a defect that did not exist** — the same shape as the four rounds §5.4 paid for. (1) Truncating the course name to 52 chars broke the join back to the course list and reported **"9 packs have no quotes"**. (2) Parsing only `- "…"` bullets measured **0 quotes on 4 packs** that have 3–6 each — this cohort also uses `> "…"` and `*   "…"`. ⭐ Both times the instrument's silence read as a finding about the data.
    - **`scripts/promote_packs.py` extended** (v2 path regression-checked unchanged — still aborts on exactly the 29 already-promoted collisions): `library_name()`/`rewrite_body()` now accept the v1 `.pack.md` naming, plus a new **`--only FILE`** allowlist. ⛔ `--only` is **load-bearing**: `--glob '*.pack.md'` alone matches all **74** v1 packs, including the 19 known-bad forbidden-path packs kept purely as evidence. It **aborts** when a listed name matches nothing, because an allowlist that silently matches nothing reads as "nothing to do" rather than "your list is wrong".
    - 🔴 **Promotion introduced a dangling reference and the first scan missed it.** `database-design-…` shipped a `## See also` pointing at **`sql-fundamentals.md`** and **`guides/dynamodb-modeling.md`**, neither of which exists — invented sibling slugs, the failure §5.2 documented. Repointed at the real `esqueca-os-orms-vamos-falar-de-sql-{a,b}.md` and `modelando-tabelas-no-dynamodb-com-single-table-design.md`, fixed **in the staged source pack and re-promoted**, not patched in the library artifact. ⭐ The first scan only matched `knowledge/coding/courses/` paths and so **missed the `guides/` dangler sitting in the same three-line block** — exactly the path-narrow grep §5.2 warned about. Scan every `knowledge/**` path, not one subtree.
    - ✅ **Promoting `database-design-…` resolved one of the 3 deliberate danglers.** Still open on purpose: `dominando-o-prisma-orm` (held above) and `dominando-o-react-hook-form` (in the 32 failing coverage).
    - ⚠️ **A third naming style is now in the library** — `-part1/-part2/-part3` (serverless, react-router) alongside 21 `-a`/`-b` files and the one prefixed odd-one-out. Kept deliberately: these packs cross-reference each other **by these filenames**, so renaming without matching the link-rewrite dangles every reference silently. Rename later as one atomic change, or leave it.

- [ ] **5.7 Recompile the held courses — STARTED 2026-08-24; `hold-9` CLOSED 2026-09-04, 27 coverage-fails remain (2026-09-05).** The 32 that failed coverage + the 9 that failed quote provenance, through `course-module-compiler` on the subscription. Running under the **corrected contract** (cap ~3.5k @ `chars/3.7`; quotes need a location reference, disfluency cleanup marked `(cleaned)`) — that is why §5.4 items 2–3 were closed first: recompiling under the old contract would have produced 41 more packs carrying the same two debts.
    - **Target list derived programmatically, not transcribed:** 32 = `pack_coverage.py --pack-glob '*.pack.md'` FAILs (51) minus the 19 that already have a `.pack.v2*.md`; 9 = the names in 5.6. **Zero overlap, 41 total, 644 lessons.** ⚠️ One name in 5.6 was elided (`construindo-e-publicando-ui-kits-…`) and the obvious completion was **wrong** — the real slug is `construindo-e-publicando-ui-kits-no-npm-com-tailwind-e-storybook`. Never expand an elided slug by guessing; `ls` it.
    - ✅ **Preflight passed on all 41:** transcripts present, `manifest.json` present, no `.pack.v2*.md` filename collision. **All 1,084 transcripts are intact on disk** (31 MB `output/` + the 48 MB mirror), so the 21.49 GB media deletion costs this phase nothing — Stage 2 reads transcripts only.
    - 🔴 **NEW ordering hazard found in 5 of the 41** — courses whose lesson files share the `1000-` prefix, so filename sort goes **alphabetical** and silently scrambles a build-along sequence. These are **different courses from the three §3.5 documented**, so the hazard was never fully enumerated: `servico-de-geracao-de-relatorios-…` (**19/19 — the worst case in the corpus**), `optimistic-updates-…` (11/19), `processamento-de-imagens-…` (11/25), `preparando-apps-react-…` (8/18), `autenticacao-no-next-js` (7/26). Their prompts must name `input/<course>/manifest.json` → `lessons[]` as the ordering authority and require the agent to confirm it used it.
    - ⛔ **A 7th copy of the token cap was found at spawn time.** `knowledge/projects/course-to-markdown/agent.md` (the file symlinked to `.claude/agents/course-module-compiler.md`, i.e. the one these agents actually run) is a **different file** from `knowledge/skills/knowledge-compiler/agent.md`, and the earlier sweep was scoped to the skill folder. Had it not been caught, all 41 agents would have compiled to the old ~2k cap. ⭐ Scope a contract grep to the **repo**, never to the folder the contract seems to live in.
    - **Batching: ≤4 concurrent** (11 batches). Session limits killed agents 3× in the 5.2 rerun, twice *after* the content was written — so on any failure **check the disk before respawning**; disk is ground truth. Prompts require both halves of a split in the SAME turn and `-a`/`-b` suffixes.
    - **Batch 1 (canary, dispatched)** deliberately exercises each risky path: `testes-automatizados-na-pratica` (41 lessons / 6 modules → split path), `servico-de-geracao-de-relatorios-…` (19/19 ordering hazard → manifest path), `construindo-um-gerenciador-de-estados-do-zero-com-react` (a synthesized-quote holdback → amended quote contract), `react-server-actions-no-next-js` (6 lessons, clean → control).
    - ⚠️ **`dominando-os-react-hooks` has exactly 1 lesson** (the YouTube outlier live #66, `nodes: []`, recording at `metadata.youtubeURL`). A one-lesson "course" is marginal as a pack — decide whether to compile it or drop it rather than auto-including it.
    - **Output convention:** `<slug>.pack.v2.md` beside the defective v1, which is **never overwritten** — it is the only evidence of the defect. `promote_packs.py` already accepts the optional v2 infix.
    - 🔴 **BATCH 1 FAILED 2026-08-24 23:28 — session limit (reset 04:20 America/Sao_Paulo; ✅ that window has PASSED, batch 1 is ready to re-run).** All four agents died in the **READ** phase. ✅ **Zero packs written, zero half-packs, zero orphan stubs, and all five v1 evidence packs untouched** (verified on disk by mtime — still Aug 5). This is the benign failure mode; the dangerous one is a limit landing *between* two split Writes. ⛔ **The disk check is not optional** — twice in 5.2 an agent had already finished its content and died during bookkeeping, so "the agent reported failure" and "no work exists" are different claims.
    - ⛔🔴 **NEW — SUBAGENTS CAN DISAGREE WITH YOU ABOUT TODAY'S DATE, AND ONE ACTED ON IT.** The `servico-de-geracao-de-relatorios-…` agent saw the harness's *"The date has changed. Today's date is now 2026-08-24"* notice arrive **inside its Skill-tool output**, correctly recognised that shape as a prompt-injection pattern (untrusted content instructing concealment), and **decided to use 2026-08-23 instead**. Its caution was well-reasoned, but the host clock confirms **2026-08-24** — so had it reached the Write, the pack would have carried a wrong `compiled:` date, in a project whose single most-repeated rule is *never invent a date*. ⭐ **Fix: state the literal date in the prompt** — `compiled: <the date the ORCHESTRATOR resolves at spawn time>`, not "today's date from your session context". ⛔ Do not copy a date out of this file: it records what was true on 2026-08-24 and is wrong every day after. Resolve it fresh (`date +%F`) each run. A date the orchestrator asserts is checkable; a date the subagent infers is not, and across 41 agents it will silently vary.
    - ✅ **BATCH 1 RE-RUN 2026-08-25 — 3 of 4 courses compiled, 4 pack files, every gate PASS.** `testes-automatizados-na-pratica` → `-a` (18 aulas, **2,483** tok) + `-b` (23 aulas, **3,528** tok), 41/41 · `servico-de-geracao-de-relatorios-…` → **3,168** tok, 19/19 · `construindo-um-gerenciador-…` → **2,698** tok, 8/8. All four carry `compiled: 2026-08-25`, confirmed by `pack_coverage.py --expect-today` (which reads Python's own clock, so it is an *independent* instrument from the orchestrator's `date +%F`). Zero banned words, zero ASCII-folding, zero orphan halves, zero stubs; all five v1 evidence packs still byte-identical at Aug 5. ⛔ Only `react-server-actions-no-next-js` remains — killed by the 07:10 session limit with **nothing written** (disk-verified).
    - ✅✅ **BATCH 1 CLOSED 4/4 — 2026-08-25b.** `react-server-actions-no-next-js` compiled on Sonnet (no escalation): **3,086 tok**, 6/6 lessons, coverage **PASS** and `--expect-today` **PASS** (4/69 courses carry today's date — exactly the four batch-1 courses). **Gate Q: 6 VERBATIM + 1 REWORDED `(cleaned)`, ZERO UNCITED** — the first pack in the corpus where every citation resolves, which is the end-to-end proof that the bare-stem rule works. The one REWORDED is a legitimate filler removal (`a maneira digamos assim mais simples` → `a maneira mais simples`), similarity 1.0, correctly marked. v1 evidence pack untouched at Aug 5, 3,747 B.
      - ⛔🔴 **`agent.md` step 5 IS WRONG ABOUT PACK PLACEMENT, and it cost a gate run.** It says *"Save it INTO the module folder"*; **all 34 v2 packs actually live at the COURSE ROOT**. The agent followed its contract faithfully and wrote to `…/react-server-actions-no-next-js/1000-conteúdo/`, where **`pack_fidelity.py` could not see it at all** (`course.glob(pack_glob)` is non-recursive) — it reported *"no pack matching"*, i.e. a silent skip, not a failure. `pack_coverage.py` *appeared* to pass only because it fell back to treating the **module** as the course, printing `PASS 1000-conteúdo` instead of the course name. ⭐ **A gate that cannot see the artifact reports success, not absence** — the tell was the course name in the PASS line, nothing else. Fix the prompt (say COURSE ROOT explicitly) and fix step 5 before batch 2.
      - ⛔🔴 **`wc -c` IS THE WRONG INSTRUMENT FOR `tokens_estimate`, AND I WALKED INTO IT.** Gate T computes `len(text)/3.7` — **characters**; `wc -c` counts **bytes**, and PT-BR packs are full of multi-byte accents. This pack: **11,721 bytes vs 11,417 characters** (304 multi-byte), i.e. **3,168 vs 3,086 tokens** — a 2.7% overstatement, in the exact same *disagreeing-instruments* family as §5.4's `words×1.33` vs `chars/3.7`. The agent's own hand-tally (no Bash in its toolset) said **2,761** and it justified that with two methods "converging within 0.03%" — both were hand-tallies of the same misread, so the agreement corroborated the error, not the value. ⭐ **Three instruments, three answers; only the one the GATE computes counts.** Declared is now 3,086 = measured.
      - ⚠️ **Declared-vs-measured is broken corpus-wide, not just here.** Gate T across all 34 v2 packs: `implementando-refresh-tokens` 2,491 measured vs **1,780** declared (−29%), `login-social-federated-a` 3,279 vs **2,300** (−30%), `sql-subqueries` 3,441 vs **2,600** (−24%). **5 packs are over the 3,500 cap by the gate's own measurement** (`paginacao-offset-…-b` 4,346 · `upload-…` 3,798 · `testes-…-b` 3,624 · `fomularios-multi-step-wizard` 3,523 · `sql-joins-json` 3,507) — and `testes-…-b` is recorded in MEMORY as a passing **3,528**. ⛔ Correct every `tokens_estimate` from Gate T at promotion; do not trust the agent-declared value or the numbers written in these notes.
      - 🔴 **NEW transcript-corruption class: whole-span LANGUAGE DRIFT, and this one is permanent.** `04-tratamento-de-erros-e-padronização-da-resposta.transcript.txt` carries **751 CJK characters beginning at char 12,106 of 16,131** — the tail ~25% of the lesson is Chinese (ASR/pipeline artifact, not course content). Unlike the `Zustand`→`Svelte` substitution class this is a contiguous span, so the compiler cannot silently repair it; the agent correctly refused to quote from it and rebuilt that stretch from the clean Portuguese either side. ⛔ **The media was deleted in the 08-23 retention pass, so it can never be re-transcribed** — this pack is the best that lesson will ever get. ⭐ **Coverage still said PASS**: a matched Curriculum line cannot see that a quarter of a lesson is unreadable. Consistency gate ≠ correctness gate, again.
      - ✅✅ **ORDINAL RESOLVER BUILT + MUTATION-PROVEN 2026-08-25c — v2 `UNCITED` 23 → 6, and 6 is the true number.** `resolve_ordinal()` reads **`manifest.json` as the ordering authority** and resolves only where the answer is *forced*: **Rule A** module qualifier (`Módulo 2, aula 01`) → filter by module then lesson number · **Rule B** no unnumbered lesson anywhere + exactly one lesson carries the number → resolve · **Rule C** *every* lesson unnumbered → manifest position. Anything else — mixed numbering, a repeated number with no module named — **refuses**.
      - ⭐🔴 **The naive version was measured WRONG on 1 of 12 before a line was written.** Scoring a prefix-only resolver against `found_in` as an oracle: **8 agree, 3 correctly refuse, 1 flat wrong** — `Lição 1` in `enviando-e-mails-com-o-aws-ses` resolves by prefix to `01-conhecendo-o-formato-mime-dos-e-mails`, while the quote lives in `1000-o-que-é-e-por-que-usar-o-ses`. That course's module 01 is entirely `1000-` (the pipeline's marker for *unnumbered*, **not** "lesson one thousand") and module 02 restarts at `01`, so filename sort inverts curriculum order. ⛔ A wrong answer here does not merely fail to help — it flips a compliant quote to **`MISATTRIBUTED`**, a fabricated alarm from the one gate that exists to detect false attribution. **Forced, or refuse.**
      - ⭐ **New verdict `UNRESOLVED_CITE` — this is the actual fix for the "wave of alarms".** `UNCITED` now means *no location reference at all* (the genuine §5.4 FAIL); `UNRESOLVED_CITE` means *a reference was given that the gate cannot resolve* (a contract nit). Conflating them is what made compliant packs read as mass fabrication. v2: **6 UNCITED + 8 UNRESOLVED_CITE + 9 resolved**. ⛔ **Zero of the 23 were ever fabrications** — every one had its text in a transcript (`found_in` non-empty). Recorded in `verifier-agent.md` so a human grader does not repeat the conflation.
      - ✅ **Safety, verified two ways.** (1) **`MISATTRIBUTED` = 0 across both corpora** after the change — no resolution landed outside `found_in`, so the resolver produced no wrong answer on real data. (2) v1 (74 packs): `NOT_FOUND` 32 and `REWORDED` 51 **both unchanged** — only bookkeeping verdicts moved (`UNCITED` 34 → 22 + 3 `UNRESOLVED_CITE`).
      - ✅ **Mutation-proven, 18 legs.** N1 ignore the unnumbered guard (reproduces the measured 1-in-12 error) kills 2 · N2 ignore the module qualifier kills 1 · N3 filename sort instead of manifest order kills 1 · N4 collapse `UNRESOLVED_CITE` into `UNCITED` kills 1 · N5 accept an ambiguous multi-hit kills 1. Every anchor count-asserted; source restored by **sha256**.
      - ✅✅ **BARE-STEM CONTRACT PROVEN ON FRESH COMPILES 2026-08-25d — 2 packs, `Gate Q 5 VERBATIM` each, ZERO flags.** `autorizacao-baseada-em-cargos-rbac-em-apis-node-js` (8 aulas, **2,635** tok) and `entendendo-a-estrutura-de-tokens-jwt-…` (5 aulas, **2,606** tok). Both coverage PASS + `--expect-today` PASS, both at the **course root**, both v1 evidence packs untouched at Aug 5. That is **3 consecutive packs with every citation resolving** (with `react-server-actions`): the contract change is proven on new work, not just retrofitted onto old.
      - ⭐ **The recompile REPAIRED a real v1 quote defect, which is the strongest evidence yet.** The v1 `autorizacao-…` pack reads `NOT_FOUND=1, REWORDED=2, UNCITED=2`; its v2 reads `VERBATIM=5, 0 flags`. ⚠️ That course had **passed** the 08-23 `quote_ngram.py` gate and was promoted on it, so this is also a second instance of *two instruments disagreeing about the same corpus* — do not read "promoted" as "quote-clean".
      - ⛔ **SELECTION ERROR worth recording: "no v2 pack" ≠ "needs recompiling".** Both courses above were picked as the two smallest lacking a v2 pack — but both are in **`promotable-9`**, already promoted to the library from v1, i.e. NOT part of the 37 held. The reconciliation: **46 courses lack a v2 pack = 9 promotable-9 (already done) + 8 hold-9 + 29 coverage-fails**, and 8 + 29 = **37**, exactly the figure the notes carried. The work was still a net upgrade (their library packs were promoted from v1 and carry the old-contract debts), but ⭐ **select the next batch from the held cohort explicitly**, never by absence of a v2 pack.
      - ⚠️ **`tokens_estimate` was wrong on BOTH again, in the same direction: 2,800 vs 2,635 and 2,750 vs 2,606** (+6.3%, +5.5%). Both agents honestly labelled it a rough estimate when told they could not measure — which is the right behaviour and much cheaper to correct than the earlier agent's fabricated "two methods converging within 0.03%". ⭐ Keep telling agents they cannot measure it; correct the field from Gate T.
      - ✅✅ **THE SHARD CEILING IS NOW MEASURED, NOT GUESSED: 87.0k SUCCEEDED in one agent 2026-08-25e.** Owner ruled out splitting `dominando-os-react-hooks` (**321,747 chars = 87.0k tokens in ONE lesson**) and it compiled clean on Sonnet: **Gate Q 3 VERBATIM, 0 flags**, coverage PASS, `--expect-today` PASS, **2,421 tok** — a **36:1** compression. Cost **258,553 subagent tokens / 33 tool uses / 11 min**, the top of the measured range. ⭐ **Revise the standing "shard over ~80k" rule: 87.0k passes, 128k kills an agent.** The true ceiling is between them; treat ~87k as proven-safe and only shard above it.
      - ⭐ **What made 87k survivable was the READ DISCIPLINE, not luck.** The prompt required one ordered pass in `offset`/`limit` chunks, ⛔ no comprehension re-reads, and compress-after-each-chunk instead of accumulating raw transcript. The agent used 8 sequential chunks plus 3 *narrow* re-reads solely to verify quote wording. The 128k failure had no such instruction and died at module 3 of 6.
      - ✅ **v2 is strictly better than v1 here:** v1 = **3,695 tok (OVER cap) with ZERO quotes**; v2 = **2,421 tok with 3 VERBATIM quotes**. Smaller *and* better sourced.
      - ✅ **A falsifiable agent claim, checked and upheld.** The agent reported avoiding a flubbed take where the instructor says *"vamos refazer esse corte… finge que nada aconteceu… apaguem os últimos 5 minutos"*. The flub is real (line 284, **84%** through the transcript); all 3 quotes sit at chars **16,892 / 80,162 / 166,627**, far clear of it. ⭐ Prefer agent claims that are *positionally checkable* — this one cost one script to verify.
      - ⛔🔴 **ALL EARLIER COURSE SIZINGS IN THESE NOTES WERE BYTE-BASED AND RUN ~3.5% HIGH.** `wc -c` counts bytes; `chars/3.7` counts characters, and PT-BR is full of multi-byte accents. Re-measured in characters, the **36 held** courses total **≈1,960k tokens** (bytes said 2,029k), and only **3** exceed the proven 87.0k ceiling: `dominando-o-prisma-orm` **94.0k** · `construindo-aplicacoes-multi-tenant` **88.9k** · `introducao-a-aws` **87.7k**. Two that looked over the old 80k rule are now comfortably inside it (`processamento-de-imagens-…` 86.3k, `inversion-of-control-…` 81.2k). Same instrument confusion as the `tokens_estimate` bug — **name the instrument**.
      - ⛔ **Correction to an earlier suggestion: `rbac-criando-um-sistema-de-controle-de-acesso-gerenciavel` is `promotable-9`, NOT held.** It is already in the library under the odd-one-out name **`jstack-live-rbac-controle-de-acesso.md`**, which is why a name-prefix grep of `knowledge/coding/courses/` reports it missing. ⭐ Check the held lists, never the library filename, to decide whether a course needs recompiling.
      - 🔴 **`dominando-os-react-hooks` breaks the shard rule: ~90k tokens in a SINGLE lesson.** The standing fix is "split at the module boundary" — there is no boundary. It needs a within-transcript topic split with each half naming its sibling, or it dies the way the 128k course did. 8 of the 46 exceed the 80k threshold; the rest are 21k–76k and fit one agent.
      - 🟡 **The 6 real `UNCITED` are all LOCATED** — 2 in `esqueca-os-orms-vamos-falar-de-sql`, 2 in `filas-na-aws-…`, 2 in `modelando-tabelas-no-dynamodb-…`. ⛔ **Do not back-fill them from `found_in`**: a citation derived from the match can never disagree with the match, so the gate would report `VERBATIM` by construction — [[feedback_a_declaration_nothing_asserts_is_decoration]]. Recompile those 3 courses under the bare-stem contract instead.
      - ✅ **Bounded by measurement, not assumption: only 2 of 1,084 transcripts** carry ≥20 CJK chars — this one, and `processamento-de-imagens-…/1000-recebendo-o-filename-…` at 32 chars (0.1%, not yet compiled). The class is **rare, not systemic**; batch 2 is not threatened by it.
    - ⭐🔴 **THE 2026-08-24 FAILURE WAS MECHANICAL, NOT BAD LUCK — a course can be too big for ONE agent, and nothing announces it.** `testes-automatizados-na-pratica` is **472,513 chars ≈ 128k tokens** of transcript. One agent cannot hold that *plus* the contract *plus* a faithful-compression working set, which is exactly why it reached only module 3 of 6. **Fix: split by module cluster across TWO agents** (01–03 ≈ 57k → `-a`, 04–06 ≈ 71k → `-b`), each reading only its own modules. Both halves passed on the first attempt. ⭐ **Measure `chars/3.7` of the transcripts BEFORE dispatch** and shard any course over ~80k; the whole corpus is 644 lessons, so this recurs. ⚠️ Cross-agent splits need the sibling filename named in *both* prompts — `pack_coverage.py` flags an ORPHANED half if one dies, which is the intended tripwire.
    - ⛔🔴 **NEW — Gate Q cannot tell "UNCITED" from "a citation it failed to PARSE", and the amended contract made that gap load-bearing.** Three valid quotes on the `testes-…` packs were reported `UNCITED` while the tool's own `found_in` field named **the exact lesson cited** (`found_in=['05-mocks']`, `resolved=None`). All 4 quotes were hand-verified against the transcripts as genuine — 3 contiguous verbatim, 1 a correctly-marked `(cleaned)` removal of the instructor's own false start (*"dado essa informação, dadas essas informações…"*). The agents had buried the lesson slug inside a long descriptive phrase, which the extractor cannot read. Since §5.4 made a missing location reference an explicit **FAIL**, across the remaining 39 courses this reads as a wave of fabrication alarms over compliant packs. **Fix both ends:** teach the extractor to find a slug anywhere in the entry (re-prove on the known-bad corpus — [[feedback_loosening_a_gate_needs_a_known_bad_rerun]]), *and* require prompts to put the slug plainly at the END of each quote entry. Same family as §5.6's two instrument bugs: **the instrument's parse failure read as a finding about the data.**
    - ✅✅ **GATE Q CITATION EXTRACTOR FIXED + RE-PROVEN 2026-08-25b — v2 `UNCITED` 26 → 23, and inert everywhere else.** Root cause was one missing direction: `resolve_cite()` tested `folded.startswith(want)`, `want.startswith(folded)` and `want in folded`, but **never `folded in want`** — precisely the buried-slug case. Repair is a longest-match, word-boundary-guarded scan for any lesson stem inside the citation, placed *before* the title word-set fallback. All 3 false alarms flipped `UNCITED → VERBATIM`, each `resolved` now naming the very lesson `found_in` had named all along.
      - ⭐🔴 **The resolution is deliberately BLIND to `hosts`.** Breaking a tie by preferring the lesson the quote was found in would manufacture `VERBATIM` out of a genuine misattribution and make `MISATTRIBUTED` **structurally unreachable** — the gate would still be green and no longer able to fail. Longest-match is deterministic and host-independent.
      - ✅ **Known-bad legs, now DURABLE — `tests/test_pack_fidelity_gateq.py`, 9 legs, `unittest` (no pytest in the venv).** The corpus regression alone proved nothing about failability: **`MISATTRIBUTED` has 0 instances in all 107 packs**, so only planted cases can exercise it. The legs: misattribution not rescued · an invented quote not laundered by a valid slug · host-blindness · prefix shadowing (`05-mocks` vs `05-mocks-avancados`) · the boundary guard · a purely descriptive cite still unresolved. ⛔ The previous round's planted fixtures were **ad hoc and did not survive**, which is why this re-proof had to be rebuilt from zero — hence tests, not a transcript.
      - ✅ **Mutation-verified — the legs FAIL on broken code.** M1 (remove the scan) kills 5 · M2 (first-match instead of longest) kills exactly the 2 host-blindness/shadowing legs · M3 (drop the boundary guard) kills exactly 1. Every mutation anchor `assert`ed to match once, and the source restored by **sha256 comparison**, not by eyeball. Corpus regression: v1 (74 packs) **byte-identical**; v2's REWORDED / STITCHED / NOT_FOUND / INCONCLUSIVE counts all unchanged.
      - 🔴 **NEW — the ORDINAL citation class is 15 of the 23 survivors, and it is still an instrument gap, not a data defect.** Remaining v2 `UNCITED` splits **15 ordinal** (`Lição 5`, `Aula 06 (…)`, `aula 07, sobre…`) · **6 with no reference at all** (the only genuine §5.4 contract violations) · **2 purely descriptive** (`mantra repetido sobre schema rígido` — unlocatable, `UNCITED` is *correct*). So the "wave of fabrication alarms" is **not** fully cleared.
      - ⛔ **Do NOT resolve ordinals by numeric prefix — measured, it breaks on 30 of 69 courses.** Prefixes are unique in only **39/69**; the other 30 collide in two shapes: modules each restarting at `01` (`api-routes-e-middlewares-no-next-js`) and the all-`1000-` courses (`autenticacao-no-next-js`) where the prefix carries no order at all. An ordinal resolver therefore **requires `manifest.json` as the ordering authority** — the same trap already recorded for the retention work. Left unbuilt on purpose: a wrong guess here manufactures false `VERBATIM`, which is the one direction this gate must never fail in.
      - ✅ **Contract half done too — the slug placement rule is now in all FIVE files that carry it** (`SKILL.md` ×2 incl. the skeleton, `course.md`, `agent.md`, `verifier-agent.md`, `course-to-markdown/agent.md`), per the standing rule that a `course.md`-only edit leaves the others stale. Rule: **end the entry with the bare transcript stem** (`— 05-mocks`); ⛔ never an ordinal, never a path, never buried mid-phrase. `verifier-agent.md` additionally FAILs a reference that is not machine-resolvable.
    - ⛔🔴 **NEW recurrence — an agent fabricated an alarming finding about a directory it had no business reporting on.** The `servico-…` agent closed its report with *"`knowledge/coding/courses/` currently has no files at all (confirmed via Glob)"*. It holds **72**. Its pack was unaffected (the claim lived only in the report), but this is the **3rd** recurrence of *a probe returning empty gets written up as a discovery* — [[feedback_agents_fabricate_alarming_findings_from_gaps]]. ⭐ Prompts should forbid claims about state outside the agent's own scope.
    - ✅ **THE DISK CHECK PAID OFF A 3RD TIME.** `construindo-um-gerenciador-…` reported failure with its last words *"Now writing the pack"* — and **had already written all 10,594 bytes** before dying in bookkeeping. Respawning on the report alone would have burned a whole agent redoing finished work. ⛔ "The agent reported failure" and "no work exists" remain different claims.
    - ⚠️ **Capacity, now measured: ~2 agents per session at this size.** The three completed agents consumed **200k / 256k / 265k** subagent tokens each. 644 lessons across the 41.
    - ⚠️ **Capacity is the real constraint: 644 lessons across the 41.** Four concurrent agents exhausted the session budget on reading alone, and the largest course (`testes-automatizados-na-pratica`, 41 lessons / 6 modules) had only reached module 3 of 6. Plan for this to span several sessions; consider dropping to **2–3 concurrent** for the 20+ lesson courses rather than 4.
    - ⚠️ `testes-automatizados-na-pratica` already carries **two** v1 packs under the third naming style (`-part1-fundamentos` / `-part2-test-doubles`). Its v2 must use `-a`/`-b`, and whoever promotes it should not assume a 1:1 file mapping against v1.
    - ✅✅ **`deploy-de-apps-react-para-producao-na-aws` RECOMPILED 2026-09-03 — the v1 quote defect is fully repaired.** Gate Q went **3 NOT_FOUND + 4 REWORDED, ZERO verbatim → 7 VERBATIM, 0 flags**; Gate T **4,254 tok (over cap) → 2,585**; coverage **9/9 PASS**; `--expect-today` PASS. v1 evidence byte-identical throughout (`sha256 b17d71ea…`, still Aug 5). This was a genuine synthesized-quote course, not an instrument artifact: v1's worst quote scored similarity **0.353** against the lesson it cited. Held cohort **36 → 35** (29 coverage-fails + 6 hold-9). Cost 150k subagent tokens / 14 tool uses / 9.5 min on Sonnet.
    - ⭐ **THE HELD COHORT IS NOW DERIVABLE, NOT TRANSCRIBED — and the bad filter's contamination is measured at 7.** `pack_coverage.py output/jstack-lives --pack-glob '*.pack.md'` reproduces the recorded **51 FAIL / 18 PASS**, and the **18 PASS are exactly `promotable-9` + `hold-9`**, which is the clean programmatic handle. Held = (51 FAIL − has-v2) + (hold-9 − has-v2) = **29 + 7 = 36** ✓. The ⛔ filter "has no v2 pack" returns **43** — it wrongly includes 7 already-promoted `promotable-9` courses: `conhecendo-o-framer` · `construindo-tabelas-com-react-table-tanstack` · `criando-um-template-de-api-com-serverless-framework-e-middy` · `database-design-…` · `dominando-o-serverless-framework` · `rbac-criando-um-sistema-…` · `react-router-v7`. ⭐ Derive the cohort from the gate each time; never re-transcribe it. The elided `construindo-e-publicando-ui-kits-…` was resolved by globbing the disk, never by guessing.
    - ⛔🔴 **`pack_coverage.py output` IS SILENTLY WRONG — the root must be the COLLECTION, not `output/`.** Given `output`, it treats the 5 top-level collection dirs (`jstack-lives`, `formacao-full-stack`, …) as *courses* and reports **`0/5 courses pass`** with 5 meaningless FAILs. The real corpus is `output/jstack-lives` (69 courses). Same family as §5.6's module-vs-course mis-report: **the gate answers confidently at the wrong tree level**, and `0/5` reads as a catastrophic finding rather than a wrong invocation.
    - ⛔🔴 **Gate Q's DEFAULT GLOB HIDES v1-only courses — it reports a PASS while matching no pack at all.** `pack_fidelity.py <course>` defaults to `--pack-glob '*.pack.v2*.md'`. Run on a course that has only a v1, it prints `1 course(s) audited, 0 carrying at least one flag` — indistinguishable from a clean pass — with the sole tell being a `!!  <course>: no pack matching *.pack.v2*.md` line above it. To audit a v1 baseline you MUST pass `--pack-glob '*.pack.md'`. Third instance of [[feedback_a_gate_that_cannot_see_the_artifact_reports_success]]; the first was placement, this one is the default argument.
    - ⚠️ **A session limit in the VERIFY phase costs a whole second pass — fold quote-checking into the first read.** Attempt 1 died having read all 9 transcripts and then *opened a separate Grep pass* to confirm quote contiguity. Disk-verified benign: **zero packs written**, v1 untouched. Attempt 2 succeeded after the prompt required selecting and verifying each quote inline while that lesson is still in context, and targeted **5–7 quotes total** rather than one per lesson. ⭐ The read-discipline rule now has a second half: *verify where you read*, not at the end.
    - ✅✅ **BATCH 2026-09-03b — 2 more `hold-9` courses recompiled, both clean.** `dynamodb-e-serverless-framework-o-par-perfeito`: Gate Q **5/5 NOT_FOUND → 7 VERBATIM** — this was *the worst quote-provenance failure in the corpus* (worst quote similarity **0.171**) and it is now fully repaired; Gate T 3,081 → **3,242**; coverage **12/12 PASS**. `construindo-e-publicando-ui-kits-no-npm-com-tailwind-e-storybook`: Gate Q 3 NOT_FOUND + 1 REWORDED → **6 VERBATIM**, Gate S **clean**, Gate T 2,861 → **2,615**, coverage **9/9 PASS**. Both v1 evidence packs byte-identical (`c37d8a73…`, `b01541e1…`). ⭐ Both agents wrote a `## See also`; **every named sibling slug was verified to resolve on disk** — no repeat of §5.6's invented-slug dangler.
    - ⛔🔴 **THE DISK CHECK PAID OFF A 4TH TIME — and this one would have cost a whole agent.** The `dynamodb-…` agent terminated on a session limit and the harness reported it as **failed**; the standing instruction in this session was to respawn it. It had already written **all 12,252 bytes** at 21:48 and died in bookkeeping. The pack was complete — all 8 sections, intact frontmatter, closing `## See also` — and passed every gate on the first check. ⛔ "The agent reported failure" and "no work exists" remain different claims, and the gap between them is now measured at 4 occurrences.
    - ⭐🔴 **NEW — A DATE ROLLOVER MID-RUN BREAKS `--expect-today`, AND THE PACK IS THE HONEST PARTY.** Both packs above were written **2026-09-03 21:48** and carry `compiled: 2026-09-03`, which their **mtime confirms is correct**. But the verification ran after midnight, so `pack_coverage.py --expect-today` (now resolving 2026-09-04) FAILs both — the gate reports a wrong date on packs whose date is right. ⛔ `--expect-today` is only meaningful **on the day of the run**; once the clock rolls, the instrument answers a different question than the one being asked. ⭐ After a rollover, verify `compiled:` against the file's **mtime**, not against today — and re-resolve `date +%F` before every dispatch, never carry one forward from earlier in a session. Same family as [[feedback_a_status_marker_that_was_true_when_written]]: the value was true when written, and only the *reader's* clock moved.
    - ✅✅ **BATCH 2026-09-04 — `documentando-componentes-com-storybook` + `dominando-o-tailwind-css`, both clean.** Storybook: Gate Q 2 NOT_FOUND + 1 REWORDED → **7 VERBATIM**, Gate S clean, coverage **14/14 PASS**. Tailwind: Gate Q 3 NOT_FOUND + 1 REWORDED (worst **0.163**) → **6 VERBATIM**, Gate S clean, coverage **15/15 PASS**, Gate T 4,061 (v1, over cap) → **2,843**. Both v1 evidence packs byte-identical (`c5ec7bc8…`, `c9277d32…`). `hold-9` is now down to **2**: `dynamodb-streams-e-search-com-algolia` and `dominando-o-prisma-orm` (sharded, dispatched).
    - 🔴🔴 **NEW CORRUPTION INSTANCE, AND GATE S IS STRUCTURALLY BLIND TO IT — `Preflight` → `Purgeflight`.** In `dominando-o-tailwind-css/1000-conteúdo/05-desabilitando-os-estilos-de-preflight-…`, the ASR renders the term **`Purgeflight` ×12 and `Preflight` ×0**, while the neighbouring lesson 04 has **`Preflight` ×11 / `Purgeflight` ×0**; corpus-wide the string appears in **exactly 1 file**. This is the `Zustand`→`Svelte` substitution class, whole-lesson. ⛔ **Gate S reported `[ok]` for this course** — it only tracks the *subject* terms (`tailwind`, `css`) going absent from one lesson, so a substituted **non-subject technical term is invisible to it**. The catch came from the compiling agent noticing the oddity and cross-checking a sibling lesson, not from any instrument. ⭐ The agent's handling was exactly right and is the rule to keep: **do not quote the corrupted span** — quoting verbatim misattributes a mistranscription to the instructor, and "fixing" it is an ASR repair, not the disfluency cleanup the contract permits. ⚠️ Media was deleted in the 08-23 retention pass, so this lesson can never be re-transcribed. ⭐ Third distinct corruption class now on record (substitution · CJK language-drift · this one), and **two of the three are invisible to every gate**.
    - ⭐🔴 **A `## See also` WRITTEN DURING A CONCURRENT BATCH CAN CITE A SIBLING'S DEFECTIVE v1.** The storybook agent Glob'd for siblings, correctly verified both existed, and cited `construindo-e-publicando-ui-kits-….pack.md` and `dominando-o-tailwind-css.pack.md` — **the v1 packs**, describing them as "staged, não revisados". Both were being repaired *in the same batch*: ui-kits' v2 had just landed and tailwind's was still in flight. So the check "does the target exist" passed while the pack still pointed readers at synthesized-quote evidence packs. Repointed both to `.pack.v2.md` and re-verified. ⭐ In a concurrent batch, a See-also must be resolved **against the batch's intended end state**, not against the disk at the moment the agent globs.
    - ⭐ **EDITING A PACK CHANGES ITS OWN TOKEN MEASUREMENT — re-derive `tokens_estimate` after ANY edit.** The See-also repoint above added 10 tokens to the storybook pack (**3,636 → 3,646** by Gate T), silently invalidating the `tokens_estimate` I had *just* corrected from Gate T minutes earlier. ⛔ The correction is not a one-time step at the end of compilation; it is the **last** step after every content edit.
    - ⚠️ **`documentando-componentes-com-storybook` ships OVER CAP at 3,646 and that is accepted, not overlooked.** It is the **3rd-largest pack in the corpus** behind `paginacao-…-b` (4,346) and `upload-…-s3` (3,798), i.e. squarely within existing precedent, and the contract explicitly forbids forcing a split to chase the number when the material is one natural cluster (a single continuous 14-lesson Button build). ⭐ The agent's own estimate was **3,650 vs 3,636 measured — off by 0.4%, the most accurate hand-estimate any agent has produced on this project**, and it flagged the likely overage itself rather than rounding down to look compliant.
    - ✅✅ **`dominando-o-prisma-orm` SHARDED AND CLOSED 2026-09-04 — the module-level ordering trap was defused, not merely noted.** Split on the curriculum arc as planned: **`-a`** = `conhecendo` + `trabalhando` (11 lessons, 77.0k in → **3,057 tok**, Gate Q **6 VERBATIM + 1 `(cleaned)`**) · **`-b`** = `relacionamentos` (5 lessons, 16.9k in → **2,514 tok**, Gate Q **3 VERBATIM + 2 `(cleaned)`**). Gate S clean on both; every `(cleaned)` entry resolves at **similarity 1.0**. **Coverage PASS at 16 lessons across the pair**, and each half names the other, so the ORPHANED tripwire is armed. v1 evidence byte-identical (`277bb28a…`). ⭐ The `-a` agent explicitly reported checking `manifest.json` and **ruling out the alphabetical trap** — the fix works when the prompt names the trap, not just the authority. ⭐ 77.0k in one agent is a second data point under the proven 87.0k ceiling.
    - ⚠️ **Two more low-confidence ASR oddities in `1000-relacionamentos`, correctly quarantined rather than "fixed".** (1) **`GraphQL` appears exactly ONCE across all 5 lessons** — *"resolver o problema com as funções de join do GraphQL"* — inside a passage otherwise entirely about hand-written SQL joins vs Prisma's query batching; GraphQL has no join functions, so this reads as a one-word mis-transcription. Verified independently: 1 occurrence in the module, 0 elsewhere. (2) A garbled span in `05-self-relation-…` while the instructor reads Prisma's disambiguation error aloud (*"a hub relation… predecessor"*, *"block owner history"*). ⭐ In both cases the agent described the rule **in its own words from the surrounding clean narration**, and quoted neither — which is the right move: a quarantined span still yields teachable content, it just cannot yield a quotation.
    - ✅✅ **`hold-9` IS CLOSED 2026-09-04 — `dynamodb-streams-e-search-com-algolia` sharded on its module boundary.** 21 lessons was far too many for one pack inside the cap, and the two modules are genuinely disjoint topics, so: **`-a`** = `01-dynamodb-streams` (14 lessons, 43.7k in → **2,761 tok**, Gate Q **4 VERBATIM + 1 `(cleaned)`** at similarity 1.0) · **`-b`** = `02-search-com-algolia` (8 lessons, 19.6k in → **2,208 tok**, Gate Q **5 VERBATIM + 1 `(cleaned)`**). **Coverage PASS at 21 lessons** across the pair, cross-links both ways, v1 byte-identical (`7513f071…`). v1 was **3/3 NOT_FOUND**. **All 9 `hold-9` courses are now recompiled; the remaining held cohort is 29, all coverage-fails.**
    - ⛔🔴 **THE DISK CHECK PAID OFF A 5TH TIME — AND THIS TIME IT SAVED A WHOLE SHARD.** Both halves above were reported **failed** on the same session limit; the `-a` agent's final words were literally *"Now I'll write the compiled pack."* **Both had already written complete packs** (10,450 B at 17:06 and 8,404 B at 17:07) — 8 sections each, intact frontmatter, proper closing `## See also`, and both passed every gate untouched. ⛔ This is the exact failure shape §5.7 already recorded once (*"Now writing the pack"* → 10,594 B already on disk), and it is now **5 for 5**: on this project, an agent reporting failure has never once meant the work was absent. ⭐ For a **shard** the stakes are higher — a limit landing between the two Writes is the one case that produces a genuine ORPHAN, so the check must confirm *both* halves, not just one.
    - 🔴🔴 **GATE S IS VACUOUS ON 18 OF 69 COURSES (26%) — IT PRINTS `[ok]` WHILE CHECKING NOTHING.** `subject_terms()` takes the tokens of the **course-directory slug** and keeps only those appearing in **≥ `SUBJECT_TERM_MIN_SHARE` = 0.8** of lessons. When no slug token clears that bar the list is **empty**, `subject_dropout()` has nothing to iterate, and the gate reports `Gate S [ok  ] subject terms []` — visually identical to a real pass. Measured: **51 courses have terms, 18 have none.** Two structural causes: **multi-topic courses** whose halves share no vocabulary (`dynamodb-streams-e-search-com-algolia`, `sql-aggregate-functions-…`, `websockets-e-cronjobs-…`, `inversion-of-control-…`) and courses whose slug words simply are not spoken (`a-melhor-stack-para-criar-front-ends-em-2024-na-minha-opiniao`). ⛔ **The blind spot includes ALREADY-PROMOTED library packs** — `conhecendo-o-framer`, `rbac-criando-um-sistema-…`, `autorizacao-baseada-em-cargos-rbac-…`, `esqueca-os-orms-…`, `sql-subqueries-…`, `sql-aggregate-…`. So the substitution class (`Zustand`→`Svelte`, `Preflight`→`Purgeflight`, `Knex`→`Next.js`) **has never been checked on a quarter of the corpus, including content already in `knowledge/`.** Same family as [[feedback_a_typo_guard_built_on_any_fires_on_empty_input]] and [[feedback_a_declaration_nothing_asserts_is_decoration]]: an empty check reads as a green one. ⭐ Minimum fix is to make an empty term list print **`[skip] no subject term met the 0.8 share`**, never `[ok]` — a gate that cannot run must say so. Any actual widening (fall back to corpus-frequent technical terms, or per-module terms for sharded courses) is a **loosening and needs a known-bad rerun** — [[feedback_loosening_a_gate_needs_a_known_bad_rerun]]. ⛔ Left unfixed on purpose: changing gate semantics mid-batch would invalidate the verdicts already recorded in this section.
    - 🔴🔴🔴 **THE MOST DANGEROUS CORRUPTION FOUND SO FAR — `Knex` → `Next.js`, a substitution that is SEMANTICALLY PLAUSIBLE.** In `dominando-o-prisma-orm`, lesson 01 renders the library correctly once (*"se eu puxar aqui o **Knex**, por exemplo, é um exemplo de Query Builder"*) then degrades to lowercase *"o **next**, né, que é o nome da biblioteca"*; lesson 02 renders it **`Next.js` ×2**: *"eu vou até abrir o **Next.js** aqui que esse pacote ele tem, o nome dele é migrations. Ó, o **Next.js** tem também"*. **Next.js has no migrations package** — the instructor is demonstrating Knex's. ⛔ Unlike `Zustand`→`Svelte` (wrong tool, same slot) or `Preflight`→`Purgeflight` (obviously not a word), this substitution names a **real, popular technology that a reader will believe**, and it is *adjacent to a true statement about migrations*. A compiler that did not notice would have written **"Next.js also has migrations"** into the library as a sourced technical claim. ⭐ Caught only because the agent cross-referenced the correct `Knex` mention **earlier in the same course** — the same cross-lesson method that caught `Purgeflight`. ⭐ **Bounded by measurement, not assumption:** corpus-wide only **3** transcripts pair `Next.js` with migrations/query-builder, and **2 of the 3 are genuine Next.js courses** where it is legitimately the subject. The class is isolated to this lesson, not systemic. ⚠️ The agent also flagged two non-load-bearing instances it did not quote (`PHP generate` for `npx prisma generate`, `Isaac ativa` for `isActive`).
    - ⭐ **A correctly-marked `(cleaned)` quote scores REWORDED at similarity 1.0 — that is the gate working, not a defect.** `dominando-o-prisma-orm.pack.v2-a` reads `Gate Q [FLAG] REWORDED=1, VERBATIM=6`; the single flag is the entry the agent marked `(cleaned)` for removing a mid-sentence `é,` filler, and it resolves `cited='08-trabalhando-com-transactions' → resolved='08-trabalhando-com-transactions'` at **similarity 1.0**. Same shape as `react-server-actions`'s legitimate filler removal. ⛔ Do not read a lone REWORDED on a `(cleaned)`-marked entry as a fidelity failure — check the similarity and whether the citation resolves before treating `[FLAG]` as a finding.
    - ✅ **First coverage-fail repaired 2026-09-05: `introducao-ao-serverless-framework` — coverage 6/10 (60%) → 10/10 PASS.** All four dropped tail lessons (`07`–`10`, two of them at **0% overlap** in v1) now carry their own `## Curriculum` lines. Gate Q 3 VERBATIM + 4 REWORDED (all legitimate, see below), Gate S clean on real terms `['serverless','framework']`, Gate T **3,291** declared == measured, in cap. v1 byte-identical (`7ce3b51f…`).
    - ⭐🔴 **BOUNDARY TRIMMING IS NOT CLEANING — agents over-label `(cleaned)`, and it dilutes the one signal that flag exists to carry.** This agent marked **6 of 7** quotes `(cleaned)`; Gate Q found only **4 REWORDED**, the other 2 matching plain VERBATIM. Diffing every marked entry against its transcript: **zero added words in all six** — every difference is a token *present in the transcript and dropped*, and the dropped tokens are overwhelmingly **span boundary** (`resto enfim então`, `escrito aqui o`, `tipo de coisa`, `levar mas`, `pacote e aí`) plus `né`/`tá` discourse markers. ⭐ **Choosing where a quote starts and stops is ordinary quotation, not disfluency cleanup**, and marking it `(cleaned)` inflates REWORDED, makes Gate Q noisier, and weakens the flag's meaning for the entries that genuinely were edited. ⛔ Err this direction, though — over-labelling is far safer than under-labelling, and the fix belongs in the contract wording (`(cleaned)` = an **interior** edit), not in a correction to this pack.
    - ⭐ **NEW EXCLUSION CLASS: the INSTRUCTOR'S OWN ERROR, distinct from ASR corruption.** In `08-configurando-o-typescript-e-o-esbuild` he says *"o esbuild leva **0,39 segundos**. Então, é absurdamente rápido. **São 39 milissegundos.**"* — internally inconsistent by **10×** (0.39 s = 390 ms). Verified in the transcript. The agent stopped its quote **before** that clause, so the wrong figure never entered the pack. ⭐ This is not a transcription defect and no ASR cross-check would catch it: the transcript is *faithful*, the speaker was simply wrong. A verbatim quote can be perfectly sourced and still propagate a false number — so quote selection has to weigh **whether the claim is true**, not only whether it was said.
    - ✅ **`renderizacao-csr-ssg-ssr-isr-ou-dsg` repaired 2026-09-05 — coverage 7/11 (64%) → 11/11 PASS**, the four dropped lessons (`08`/`09`/`10`/`12`) each restored to the `## Curriculum` spine and the missing `## How to apply` section restored. Gate Q **7 VERBATIM, 0 flags**; Gate T **2,954** (v1 declared 1,800 against ~859 actual, +210%); v1 byte-identical (`09e128d8…`). Gate S flags `'ssg'` absent from `01-…-csr` — benign and expected: a CSR lesson need not mention SSG.
    - 🔴🔴🔴 **A THIRD EXCLUSION CLASS, AND THE ONLY ONE NO CROSS-CHECK CAN RESOLVE: the acronym is used CONSISTENTLY WRONG.** In `08-ferramentas-e-casos-de-uso-para-o-**csr**`, the spoken acronym is `SSG` (×8) throughout while the *content* is unambiguously CSR: *"Para **SSG** … a nossa quase falecida **Create React App** … o nosso famoso **CRA**"*, *"a minha recomendação é **Vite**"* (both CSR/SPA bundlers, not SSG generators), and decisively *"Eu posso usar um **SSG** … eu levo tudo pro lado do meu **cliente**"* — moving everything client-side **is** the definition of CSR. Lesson `04-prós-e-contras-do-csr` shows the same pattern (`SSR` ×6 inside CSR argumentation).
      - ⛔ **Cross-lesson comparison — the method that caught `Purgeflight` and `Knex`→`Next.js` — CANNOT resolve this one.** Those were *inconsistent* (one lesson right, another wrong), so a sibling lesson supplied the correct term. Here the wrong acronym is used **self-consistently within the lesson**, so nothing internal contradicts it. Only **domain knowledge** resolves it: knowing CRA/Vite are not SSG tools, and that "tudo pro lado do cliente" is CSR.
      - ⚠️ **It is also genuinely undecidable between ASR error and instructor misspeak** — and the instructor says so himself, *in this very lesson*: *"em optar por usar um SSG e um SSR da vida, né, um CSR, **sempre me confundo**, né?"* (verified verbatim). ⭐ The agent's handling is the rule to keep: **quote nothing from lessons 04 or 08**, and compress their content from the *unambiguous* signals — tool identity (Vite/CRA are real whatever they are labelled) and the manifest's authoritative lesson title — rather than the spoken acronym.
      - ⭐ **Raw acronym counts are NOT a corruption signal; context is.** I suspected `13-…-dsg` too (SSG ×5 in a DSG lesson) and was **wrong** — it carries `DSG` ×8 and every `SSG` mention is deliberate contrast (*"o DSG segue a mesma linha do SSG"*, *"qual é a diferença do DSG pro SSG?"*). A frequency heuristic would have flagged a clean lesson and, worse, would miss lesson 08 whenever the wrong term happens to dominate legitimately.
    - 📋 **THE REMAINING 29 (all coverage-fails) ARE NOW FULLY MAPPED — 1,579,054 tok, measured in characters.** Sizes run **30.3k → 88.9k**. Hazards enumerated up front so no batch rediscovers them:
      - 🔴 **2 exceed the proven 87.0k single-agent ceiling and MUST be sharded:** `construindo-aplicacoes-multi-tenant` **88.9k** (28 lessons, 3 modules) · `introducao-a-aws` **87.7k** (25 lessons, 4 modules). Both have real module boundaries, so shard on those, not arbitrarily.
      - 🔴 **5 have a manifest-vs-filename ordering DIFF:** `api-routes-e-middlewares-no-next-js` · `optimistic-updates-uis-otimistas-com-react-query` · `preparando-apps-react-para-producao-…` · `autenticacao-no-next-js` · `processamento-de-imagens-assincrona-…`. Their prompts must name `manifest.json` as the authority **and name the trap explicitly** — the prisma run proved the agent confirms it was ruled out only when the prompt says what the trap is.
      - 🔴 **4 carry the MODULE-level `1000-` collision** (every module folder prefixed `1000-`, so the module walk sorts alphabetically): `upload-para-o-s3-com-lambda-functions-e-presigned-urls` · `api-routes-e-middlewares-no-next-js` · `autenticacao-com-refresh-tokens-no-react` · `preparando-apps-react-para-producao-…`. ⚠️ **`api-routes-…` and `preparando-apps-react-…` carry BOTH hazards.**
      - ⭐ **The defect signature is TAIL TRUNCATION, and it is highly consistent.** Spot-checked two: `renderizacao-csr-ssg-ssr-isr-ou-dsg` **7/11 (64%)**, missing `08`/`09`/`10`/`12` *and* the whole `## How to apply` section, declaring `tokens_estimate: 1800` against **~859 actual (+210%)**; `introducao-ao-serverless-framework` **6/10 (60%)**, missing `07`–`10`, i.e. the entire tail, **two of them at 0% overlap** — those lessons never reached the compiler at all. ⭐ So the load-bearing instruction for this cohort is different from `hold-9`'s: **every lesson must get its own `## Curriculum` line**, and the tail is where to spend the attention.
    - ⛔🔴 **NEW HAZARD CLASS — the `1000-` ordering trap also exists at MODULE level, and `dominando-o-prisma-orm` has it.** The five cases enumerated above are *lesson* files colliding on the `1000-` prefix inside one module. Prisma's **three module folders** all carry it — `1000-conhecendo-o-prisma`, `1000-relacionamentos`, `1000-trabalhando-com-o-prisma` — so a filename sort orders them **alphabetically**: `conhecendo → relacionamentos → trabalhando`, while the manifest's curriculum order is `conhecendo → trabalhando → relacionamentos`. A filename-sorted compile therefore teaches **relationships before the project setup and schema** — 13 of 16 lessons out of position. The lesson SET is identical (16 = 16), so nothing is missing and **no count-based check can see this**; only comparing the two orderings does. ⭐ The standing "manifest is the ordering authority" rule must be applied to the module walk, not just the lesson sort within a module.
    - ⚠️ **`dominando-o-prisma-orm` shard plan (93,965 tok — the only remaining `hold-9` over the proven 87.0k ceiling).** Module sizes: `conhecendo` **22.9k** · `trabalhando` **54.1k** · `relacionamentos` **16.9k**. Split on the curriculum arc, not the alphabet: **`-a` = conhecendo + trabalhando (77.0k**, under the proven ceiling, the "learn + build" arc) · **`-b` = relacionamentos (16.9k**, self-contained). Both prompts must name the sibling filename — `pack_coverage.py` flags an ORPHANED half if one dies, which is the intended tripwire. Also carries 2 CJK chars (below the ≥20 contamination threshold; benign).
    - ⚠️ **Two observations for the human review at promotion.** (1) The v2 `## See also` is **empty** — *"Nenhum cross-link identificado"* — where v1 carried 5 entries incl. two real sibling courses. Honest (⛔ no invented danglers, the §5.6 failure) but a usefulness regression; back-fill by hand at promotion. (2) Gate S flags `'deploy'` absent from `05-entendendo-o-que-são-cdns-…` on **both v1 and v2** — a property of the transcript (a whiteboard CDN lesson whose unique terms are `Austrália`/`Curitiba`/`PoP`/`Edge Locations`), **not** a substitution signature. Benign, and expected to recur on every conceptual lesson.

---

## Cross-cutting checklist

- [ ] Process only content the user is entitled to access and permitted to retain offline.
- [ ] Use exported sessions only; never automate credentials, defeat access controls, or broaden access.
- [ ] Stop and mark `skipped-drm` when commercial DRM is detected; never decrypt it.
- [ ] Keep cookies, tokens, signed URLs, media, transcripts, and manifests with sensitive payloads out of Git.
- [ ] Use `.venv` for every Python test and production command.
- [ ] Keep API/catalog polling polite and sequential; resume instead of restarting.
- [ ] Use Gemini for code-heavy Portuguese courses; use Groq only when jargon fidelity is not material.
- [ ] Never auto-promote Stage 2 artifacts into the knowledge library.

## Open questions still to decide

- [ ] **DesignBoost acquisition scope:** download one selected course, one catalog, or all four catalogs after adapter verification?
- [ ] **Skool acquisition scope:** download/transcribe only the supplied `Equação de Valor` course or add group-wide course discovery first?
- [ ] **Attachments:** should native Skool files and DesignBoost tool/resource downloads be first-class Stage 0 artifacts, or are descriptions + external links sufficient?
- [ ] **JStack Stage 2 granularity:** which projects/lives contain reusable knowledge worth compiling versus archival transcripts only?
- [x] **Retention — ✅ DECIDED 2026-08-23b: delete the media, archive the transcripts.** 21.49 GB of source `.m4a` removed (1,615 files, `input/` 22.18 → 0.69 GB), keeping only the 3 courses phase 5.5 still needs. Transcripts, staged packs, review evidence, and the `input/` metadata are mirrored to `knowledge/backups/course-to-markdown/` in the **private** parent repo. See item 7 under *Next session* for the two traps this nearly hit. ⚠️ **Accepted consequence:** the jargon audit only ever sampled **19 of 1,084** lessons (1.75%) in `--reference` mode, so undetected MiMo substitution may remain in the other 1,065 — and re-transcribing any of them is now impossible outside the 3 kept courses. Recompilation from transcripts is unaffected.

## Open inputs still needed from the user

- **None for the current adapter/JStack completion work.** Re-export the relevant cookie file only if a session begins returning 401/403.
- **A scope choice is needed before bulk-downloading DesignBoost or Skool.** Adapter verification alone does not imply authorization to download every newly visible course.
