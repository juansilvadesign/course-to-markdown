---
name: Course-to-Markdown Project Memory
description: Working memory for the course-to-markdown three-stage pipeline — architecture, decisions, gotchas, and state.
type: project
---

# course-to-markdown — Project Memory

Turns owned course videos into `knowledge-compiler` packs. Built 2026-06-19 (MVP). See [`README.md`](README.md) for usage; this file holds the decisions, gotchas, and state a future session needs.

## ▶ Live resume state

> **Migrated out of the global memory router 2026-08-16.** The router keeps a one-line stub pointing here; ⛔ new detail lands in this file, not in the router. The forbidden-path incident itself is documented at length in [`CLAUDE.md`](CLAUDE.md) § *Common mistakes* — this block is only the open state.

## ▶ Resume (checkpoint 2026-08-24)
- **Project:** `knowledge/projects/course-to-markdown` (submodule; ⚠️ **public** — technical state only here)
- **Doing:** ✅ committed + pushed (parent `8fac90f`, sub `92ca712`). ✅ **Both 5.4 contract debts CLOSED 2026-08-24** — token cap and quote contract. Next up: the 41 held courses.
- **Next step:** **Re-run batch 1 of the 41-course recompile — the 2026-08-24 23:28 session limit has RESET and is no longer blocking.** ✅ Nothing was written and no v1 evidence was touched (died in the READ phase), so batch 1 re-runs clean from scratch. ⛔ Before respawning ANY batch, check the disk — a failed agent report is not proof no work exists. 41 held courses = 32 failing coverage + 9 on synthesized quotes; running under the corrected contract, which is why the decisions went first. ✅ **All 1,084 transcripts across 69 courses are intact on disk** (31 MB `output/` + the 48 MB mirror), so the 21.49 GB media deletion costs nothing here — Stage 2 needs only transcripts. ⛔ Fan out in batches of **≤4**; session limits hit 3× last time, and disk is ground truth when one dies.
- **Key paths / IDs:** `TASKS.md` §5.4 items 2–3 (the two closed decisions, with the unit-mismatch write-up) · §5.6 (gate results + the 9 held) · `knowledge/backups/course-to-markdown/` · `evidence/quote_ngram.py` · `scripts/promote_packs.py --only FILE`
- **Open / blockers:** (1) the **32** courses failing coverage (median 58%) — recompile or drop; (2) the **9** held on synthesized quotes; (3) §5.5's 3 term-loss lessons — the only work the kept media serves; (4) the **23 uncited quotes** in the promoted 29 — now the sole surviving quote debt, since the 14 rewordings were legitimized by the amended contract.

### ✅ Both 5.4 contract debts CLOSED 2026-08-24 — and the cap one was a UNIT bug, not a number bug

- ⛔🔴 **"25 of 29 exceed the ~2k cap" was measured in units the enforcing gate never computed.** `verifier-agent.md` gated on `wc -w × 1.33`; `pack_fidelity.py`/`pack_coverage.py` measured `chars / 3.7`. Same 91 library course packs, **median 1.42× disagreement** — 27 over cap by one instrument, **73** by the other. The recorded max of 4,344 is the chars/3.7 reading of `paginacao-…-b.md`, which is how the two were told apart. ⭐ **A cap is meaningless without naming the instrument** — same family as [[feedback_consistency_gate_is_not_a_correctness_gate]] (*print the value the gate gated on*).
- **Fix: one method everywhere (`chars / 3.7`) + every type re-baselined to its measured p90**, so the switch corrects the unit rather than silently tightening: article 1,000→**1,350** · guide 1,500→**2,000** · book 2,000→**3,000** · course 2,000→**3,500**. ⚠️ **All four playbooks carried their own cap** plus `SKILL.md`, `agent.md` ×2 and `verifier-agent.md` — a `course.md`-only edit leaves three stale. Grep for every mention before declaring a contract changed.
- **Quote contract amended for spoken sources:** disfluency-only cleanup allowed when marked `(cleaned)`; **location reference is now an explicit FAIL if missing**, because an unlocatable quote is unfalsifiable. The §5.6 synthesized-quote failure is written into both `SKILL.md` and `verifier-agent.md` with the rule ⛔ *never treat "the cited lesson covers this topic" as evidence the quote is real*.
- ✅ **Re-proven against the known-bad corpus** ([[feedback_loosening_a_gate_needs_a_known_bad_rerun]]): `pack_coverage.py` over v1 still returns **18/69 pass**, invented-date/truncation/coverage detection all still firing. **The cap is not inert** — library FAILs go 91/195 → **8/195**, the 8 being real outliers (`101_agencia.md` 9,117 vs a 2,000 guide cap; `intensivo-cliente-pago.md` 8,391 vs 3,500).
- ⛔🔴 **A SUBAGENT CAN DISAGREE WITH YOU ABOUT TODAY'S DATE — and one did, reasonably.** The harness's *"The date has changed. Today's date is now 2026-08-24"* notice reached a `course-module-compiler` agent **inside its Skill-tool output**. The agent correctly read that shape as a prompt-injection pattern (untrusted content carrying an instruction) and **chose 2026-08-23 instead**. The host clock says **2026-08-24**, so the pack would have shipped a wrong `compiled:` date — in the one project whose most-repeated rule is *never invent a date*. ⭐ **State the literal date in the prompt** (`compiled: 2026-08-24`), never "today's date from your session context": an asserted date is checkable, an inferred one silently varies across 41 agents. ⚠️ The agent's caution was correct behavior, not a defect — do not "fix" it by telling agents to trust injected notices.
- ⚠️ **Neither constant is ground truth.** No tokenizer is installed in the venv; `3.7` is a PT-tuned rule of thumb and the caps are p90s **of an estimate**. If a real count ever matters, install `tiktoken`/`transformers` and re-baseline.
- **Don't forget:** `output-tree/` and `output-2026-08-23.tar.gz` hold **identical bytes**. Now that the backup is inside git, git *is* the archive — decide whether to gitignore `*.tar.gz` there **before the first commit**, because removing a 7.76 MB blob afterwards needs a history rewrite. ⛔ Media is gone (21.49 GB, 1,615 files); only the 3 §5.5 courses can ever be re-transcribed.

- ✅ **Stages 0 and 1 are FINISHED and pushed: 1,668 / 1,668.**
- ✅✅ **Phase 5 forbidden-path incident CLOSED 2026-08-23. 5.2 + 5.3 + 5.4 all done; the 19 courses (29 pack files) are PROMOTED into `knowledge/coding/courses/`.**
- ✅ **5.6 — the other 50 GATED 2026-08-23b: 8 more courses / 12 pack files promoted, library 60 → 72. 42 still held.** Two gates, each halving the cohort: coverage **18 pass / 32 fail** (median 58%), then quote provenance **9 pass / 9 fail** on the survivors. Full detail + the held list in `TASKS.md` §5.6. 🟡 The 12 library files, the extended `promote_packs.py`, and `knowledge/backups/` are **uncommitted**.
- ✅ **RETENTION CLOSED 2026-08-23b — media deleted, transcripts archived.** `input/` **22.18 GB → 0.69 GB** (1,615 files); kept the 55 media files of the 3 courses §5.5 needs. Everything gitignored-and-irreplaceable now mirrors to `knowledge/backups/course-to-markdown/` in the **private** parent repo.
    - ⛔🔴 **The transcripts were NEVER in git and the assumption that they were nearly cost all 1,688 of them.** `.gitignore:13` `/output/*` — a deliberate hard rule, because the `course-to-markdown` repo is **public** and this is paid course content. The promoted packs are the compressed ~3% and **cannot** rebuild a transcript. ⭐ The near-miss inverted the value ranking exactly: transcripts are **0.1%** of the media's size and the only artifact that can rebuild or verify a pack, while the 22 GB of media served no open task but three lessons. **Delete the big cheap thing, keep the small irreplaceable one.**
    - ⛔🔴 **`input/` is NOT just media — an `rm -rf` of the course dirs destroys the ordering authority.** It also holds **74 `manifest.json`** (the *only* thing establishing curriculum order: 6 modules across 3 courses name every lesson `1000-<title>`, so filename sort silently goes alphabetical) and **1,705 `.description.md`** sidecars carrying the author. **1.85 MB total.** Delete by **media extension**, never by directory.
    - ⭐ **Gate an irreversible delete on a positive assertion, not on "the backup ran".** The transcript count was asserted equal across **three** independent locations (live tree, mirror, tarball interior) before a single file was removed; the mirror was separately proven a real copy by **inode**, not by file count — [[feedback_cp_r_of_a_symlink_is_not_a_backup]].

### ✅ 5.4 COMPLETE — reviewed against the TRANSCRIPTS, then promoted, 2026-08-23

- 🔴 **The task as written ("compare v2 against v1") could not have worked, and this is the reusable lesson.** v1 read a **median 37%** of each course and **7 of the 19 v1 packs are 358–1,286 B truncated stubs** that die mid-sentence inside the TL;DR. Agreement between v2 and v1 therefore corroborates *the partiality*, not the pack — the same family as [[feedback_agreement_with_a_known_partial_range_corroborates_the_partiality]]. The authority had to be the transcripts; v1 survived only as a regression diff, which found nothing lost.
- **New instrument: `scripts/pack_fidelity.py`** (sibling to `pack_coverage.py`). **Gate Q** — every `## Quotes worth keeping` entry must trace to a transcript, classified VERBATIM / REWORDED / MISATTRIBUTED / STITCHED / NOT_FOUND / INCONCLUSIVE. **Gate S** — subject-term dropout, the substitution class a v1↔v2 diff structurally *cannot* see. **Gate T** — token measurement, recorded never graded.
- ✅ **Zero fabrications in 53 quotes across all 19 courses. Zero transcript corruptions propagated into any pack.**
- 🔴 **The compiler SILENTLY REPAIRS corrupted transcripts** — `Svelte ×6`→`Zustand`, `Zustend ×16`→`Zustand`, `form setval`→`form.setValue` (that one it even annotated in the pack). Good for the packs; it also means **transcript corruption is invisible downstream**, so the transcript is the only place it can ever be caught. `Zustend ×16` in `12-manipulação…immer` is **newly found** — the previously recorded `Svelte` case was not the only one.
- 🟡 **Promoted WITH known quote-contract debt.** `SKILL.md` requires *"Verbatim, with location reference."* Actual: **13 VERBATIM · 14 REWORDED · 23 UNCITED · 1 STITCHED · 1 INCONCLUSIVE.** Every rewording is **spoken-disfluency cleanup**, hand-verified: a fixed misspeak (`criou`→`crie`), a pluralised verb (`ela seja`→`sejam`), a removed stutter (`um um uma`→`uma`). Meaning preserved throughout — debt, not wrong knowledge.
- **`tokens_estimate` was wrong on 25 of 29 packs**: **17 under-stated, 8 OVER-stated**, 4 within ±50 (worst +979). Corrected from measured character count at promotion. ⛔ "Agents guess low" is a **tendency, not a law** — 8 guessed high.

#### ⭐ Four false-positive rounds the instrument paid for — every one caught by re-measuring, never by the tool's own report

⭐ In all four the **wrong answer was the alarming one**, which is the direction that survives review — same shape as [[feedback_agents_fabricate_alarming_findings_from_gaps]].

- 🔴 **The elision regex missed a BARE `...`** (it handled `[...]` and `…` only). First run reported **6 quotes as fabricated**; 5 had both fragments verbatim in the transcript, one sentence apart. Nearly filed a fabrication charge against two clean packs.
- 🔴 **Square brackets do two OPPOSITE jobs.** `[...]` = text removed (sides are *not* contiguous → split); `[o índice]` = editorial insertion never spoken (sides *are* contiguous → drop). Conflating them made 2 more quotes unmatchable. ⚠️ Consequence to accept: Gate Q now **cannot validate a claim made inside brackets** — a compiler could assert something false there and the gate would never object.
- 🔴 **Similarity diluted by window size.** Scoring a fragment against a window 4× its length read two near-identical passages as 0.37. Fix: score **coverage of the fragment**, not symmetric similarity → the same passages score 0.92 and 1.0.
- 🔴 **The fuzzy anchor could be a zero-frequency word.** A missing plural is simultaneously the rarest word *and* the absent one, so anchoring on it guaranteed 0.0 for exactly the cases the function exists to explain.
- ⭐ **Every fix was re-validated against a planted known-bad pack AFTER the change**, because each one loosened the gate: an invented quote still scores 0.214 → `NOT_FOUND`, a real quote on the wrong lesson still reads `MISATTRIBUTED`, elision abuse reads `WEAK_MATCH` not a false `VERBATIM`, and `Svelte ×6` still surfaces after Gate S tightened. ⛔ [[feedback_loosening_a_gate_needs_a_known_bad_rerun]].
- ⛔ **Gate S precision degrades on multi-word course slugs.** On the 25-lesson cognito course it emitted 8 findings, **all benign** (a setup lesson not saying "cognito"). Filtering out candidates that appear in the lesson's own title removed the worst of it (`CORS ×10` in a lesson titled `…-de-cors`), but read Gate S as a **lead list, not a defect list**.

#### Promotion mechanics — `scripts/promote_packs.py`

- **Library naming was NOT invented:** multiple packs' own `## See also` entries already link to `knowledge/coding/courses/<full-slug>.md`, so that is the convention (flat, no prefix, no `.pack` infix). Splits → `<slug>-a.md` / `<slug>-b.md`. ⚠️ The single earlier precedent `jstack-live-rbac-controle-de-acesso.md` uses a *prefixed, shortened* name and is now the odd one out.
- **26 intra-pack cross-references repointed** from staging filenames to library names — split halves cite each other by filename, and every one of those would have dangled silently.
- ⛔ **`output/` was not touched.** All 102 staged packs and all 19 v1 packs remain as defect evidence.
- **3 references still dangle ON PURPOSE** — `database-design-…`, `dominando-o-prisma-orm`, `dominando-o-react-hook-form` are among the other 50, never recompiled, never promoted.

### ✅ Stage-2 rerun COMPLETE — 19/19, 272/272 lessons, 2026-08-21

- ✅ **All 19 courses recompiled and verified: `19 pass / 0 fail`, 272 lessons, 29 pack files** (`pack_coverage.py … --pack-glob '*.pack.v2*.md'`). Zero packs carry the invented `compiled: 2024-*` date. Coverage went from a 22–66% (median 37%) cohort to **100% on every course**.
- ⛔ **Still staged, NOT blessed.** 5.4 (human v1↔v2 comparison) is the remaining gate; promote nothing until it runs. A matched Curriculum line proves no lesson was silently dropped — it does **not** prove the body is faithful.
- ✅ **(RESOLVED 2026-08-24 — see the Resume block's "both 5.4 contract debts CLOSED".)** ~~🔴 The ~2k token cap in `course.md` is mis-set for these lives: 27 of 29 packs exceed it~~ (min 1,933 · median 2,641 · max 4,383), *including packs that are already halves of a split*. 27/29 was not 27 agents misjudging. Cap raised to **~3.5k for `type: course`** and the measurement unified on `chars / 3.7`; these very numbers were the chars/3.7 reading all along, which is how the unit mismatch was found. ⛔ don't force three-way splits to satisfy a number the corpus disagrees with.

#### ⭐ Agent-supervision failures this rerun paid for — all caught by re-measuring, never by the agent's own report

- 🔴 **Agents invent a "missing lesson" out of a platform NUMBERING GAP.** These courses skip numbers (`01,02,04…`) because the *platform* skips them — the manifest shows the same gap. Two agents wrote `(não capturado no lote de transcrição — ausente do output)` into the pack as a curriculum line, **fabricating a Stage-1 transcription failure that never happened**; a third nearly did. ⭐ The tell is that the wrong answer is the *alarming* one, so it survives review. Fix: compare transcripts against the **manifest**, not against a contiguous number line, and say "complete" out loud. Prompts must ban the words `não capturado`/`ausente`/`missing`.
- 🔴 **`manifest.json` lives under `input/<course>/`, NEVER under `output/<course>/`.** Prompts that named the output path sent agents hunting for a file that cannot be there; they silently fell back to filename sort. It caused no damage this time (all 3 collision-prone courses were checked against the real manifest and matched), but the ordering authority was unenforceable for half the run.
- 🔴 **A `tokens_estimate` an agent did not MEASURE is a guess, and it guesses low.** Corrected by hand on 4 packs (2000→3480, 2250→3600, 2450→3491, 5000→3885). Agents without shell access hand-estimate character counts — one was accurate (15,900 vs 16,219 actual), so require the measurement and re-check it.
- 🔴 **A session limit kills an agent between its two split Writes**, leaving a Part 1 that promises a Part 2 nobody wrote — and by file *existence* the course looks compiled. Hit **3 separate times**. ⭐ Cheaper than respawning: check the disk first — twice the agent had already finished the content and died during bookkeeping. Prompts must say "write BOTH halves in the SAME turn".
- ⛔ **Split halves must be `-a`/`-b`.** One agent used `-parte2`, which defeats the orphan detector entirely (it keys on the suffix). Another left a third **stub** file at the unsuffixed `.pack.v2.md` path pointing at its own halves — the glob picks it up and it fails as truncated. Name the convention in the prompt.
- ⛔ **Agents fall back to `author: JStack` when the description sidecar is one directory away.** `input/<course>/00-*.description.md` carries `**Autor(es):** Mateus Silva`. Point at it explicitly.

#### 🔧 `pack_coverage.py` fix — a filename cannot carry punctuation

- 🔴 **The verifier reported a lesson that WAS in the spine as unreached.** `03-criando-a-ui-com-o-shadcnui.transcript.txt` reduces to the single token `shadcnui`; the pack correctly writes `shadcn/ui`, which folds to `shadcn`+`ui` — overlap **0%**, coverage 11/12. **The tool was wrong, not the pack.** Fix: also match a token against a *squashed* (all-alnum) form of each curriculum line, guarded at `len>=6`. Same measurement-artifact class as the jargon audit's whole-word matching over a bilingual corpus.
- ⭐ **Validated the fix the only way that means anything: re-ran the known-bad v1 corpus and confirmed all 19 are STILL flagged** (0–86%, most under 60%). A gate change that makes a failure disappear must be proven not to have made the gate weaker.

### ▶ Stage-2 rerun — history (started 2026-08-19)

> ⚠️ **SUPERSEDED by the block above.** The counts here ("4 of 19 done", "15 remain") are a mid-run snapshot from 2026-08-19 — kept for the method and the before→after numbers, not as state. Current state: **19/19 done**.

- ✅ **`scripts/compile_lives_stage2.py` DELETED — `git rm` + commit `b48f097`, NOT pushed.** ⚠️ The old note said "do not commit it"; it **was already committed and pushed** (`83503a7`), so removal was a `git rm`, not an `rm`. It survives in history — treat the file as reachable, not gone. No hardcoded key in it.
- **Recompile model: one `course-module-compiler` agent per COURSE (not per module), ≤4 concurrent, Sonnet with `ESCALATE_TO_OPUS` left available.** No agent has escalated so far — instructors name their own frameworks, so the Opus triggers don't fire.
- **New packs are written BESIDE the old ones as `<course>.pack.v2.md`** (`.v2-a`/`.v2-b` when split). ⛔ `output/` is **gitignored**, so overwriting a defective pack destroys the only copy — the v2 suffix keeps the bad pack as evidence until a human compares and swaps.
- ✅ **4 of 19 done and independently verified**, coverage measured before → after:
  | course | before | after | note |
  |---|---|---|---|
  | `sql-subqueries-transactions-e-triggers` | 45% | **100%** | 11/11 |
  | `gerenciamento-de-estados-com-zustand` | 42% | **100%** | 12/12, `unknown` restored |
  | `login-social-federated-com-aws-cognito` | 40% | **100%** | 25/25, split 2-way |
  | `sql-joins-json-no-postgres-e-indices` | 62% | **100%** | 8/8 |
- **15 remain.** 4 were in flight at checkpoint: `login-social-via-magic-link…`, `formularios-dinamicos…`, `enviando-e-mails-com-o-aws-ses`, `websockets-e-cronjobs-serverless-na-aws`. 11 not yet started.
- ⛔ **Promote nothing** into `knowledge/<domain>/courses/` until all 19 are recompiled AND a human compares v2 against v1 (5.4). The v2 packs are staged, not blessed.

### ⭐ Findings this rerun paid for — do not rediscover

- 🔴 **A shared `1000-` filename prefix means filename sort ≠ curriculum order.** 6 modules across 3 courses name *every* lesson `1000-<title>`, so sorting by filename yields **alphabetical** order and silently scrambles a build-along sequence. Nothing announces it — the pack looks complete. **The ordering authority is `input/<course>/manifest.json` → `lessons[]`** (platform order, match by `title`). Affected: `enviando-e-mails-com-o-aws-ses/01-…`, `login-social-federated-com-aws-cognito/01-,03-,04-`, `websockets-e-cronjobs-serverless-na-aws/02-,03-`.
- 🔴 **The jargon audit's "3 of 19 lose a term" is a FLOOR, not a ceiling.** `output/jstack-lives-gemini-ref/` holds **one reference lesson per course** (19 of 1,084 ≈ **1.75%** sampled), so `--reference` mode is structurally blind everywhere it has no counterpart. Proof: a compiling agent reading the full text found **"Zustand" rendered as "Svelte" ×6, with "Zustand" appearing 0×**, in `gerenciamento-de-estados-com-zustand/1000-conteúdo/11-configurando-e-utilizando-a-devtools` — a lesson the baseline never covered. ⭐ This is a *different* corruption class than the documented one: not term **loss** (a term present in ref, absent in main) but **substitution by another real product name**, which reads perfectly and defeats a diff that has nothing to diff against.
- ✅ **A corpus-wide grep for `svelte` returns only 4 files**, and 3 are legitimate (Tailwind comparison, Next-auth intro, SSG tooling). So *that* substitution is narrow — but it is only detectable by reading, not by the audit.
- 🔴 **The substitution class is BROADER than one term — four confirmed shapes, found only by agents reading full text:**
  | shape | evidence | strength |
  |---|---|---|
  | type → wrong type | `unknown` → `any`, 8/8 in one lesson | unambiguous |
  | library → other library | "Zustand" → "Svelte" ×6, correct term 0× in a lesson *about* Zustand | unambiguous |
  | **own brand → invented brand** | **"JStack" → "GeekStack"/"DevStack" in 20 files across 15 COURSES** (correct in 131 files ⇒ **~13% corruption rate**) | unambiguous, and the widest |
  | framework name intrusion | "Vue" in a React-only course | **mixed** — 1 of 3 was a legitimate comparative aside |
  ⭐ The brand case is the most useful detector: the truth is knowable independently (`manifest.json` → `"platform": "jstack"`), and it spans **beyond the 19-cohort into already-promoted packs**.
- 🔴🔴 **⛔ THE "3 OF 19 LOSE A TERM" FINDING IS MOSTLY A MEASUREMENT ARTIFACT — 2 of the 3 are false positives.** `jargon_audit.py` (and my own reproduction of it) counts **whole-word English tokens** against transcripts of a man speaking **Portuguese about English APIs**. Verified 2026-08-19 by counting substrings and PT forms as well:
  | lesson | audit said | ground truth | verdict |
  |---|---|---|---|
  | `…zustand/06-configurando-o-zustand-com-o-typescript` | `unknown` ×8 → ×0 | main really says `any` | ✅ **GENUINE** — a semantic type inversion |
  | `…multi-step-wizard/08-integrando-o-formulário…` | `context` ×3 → ×0 | **both** files have 7 "context" substrings + 4 "contexto"; the ref merely writes 3 **standalone** where main writes them **attached** (`useContext`, `StepperContext`) | ⛔ **ARTIFACT** |
  | `…websockets/…refactor-abstraindo-no-hook-usewebsockets` | `callback` ×2 → ×0 | main's sole hit is **`useCallback`** — the real React hook, correct for a hook-refactor lesson. The **reference** has generic "callback" ×2 and **no `useCallback` at all** | ⛔ **ARTIFACT, and INVERTED** — the ref is the *worse* source here |
  ⭐ **A whole-word count of an English term is not a measure of a bilingual transcript.** `\bcontext\b` misses `contexto`, `useContext`, and `StepperContext` — three ways the same concept legitimately appears. ⛔ Before calling a term "lost", count the **substring** and the **target-language form**, and check whether the "missing" token is actually embedded in a longer *correct* identifier.
- 🔴 **⛔ CORRECTION — the Gemini reference tree is NOT a clean source.** Earlier in this same rerun I recorded that pointing an agent at `jstack-lives-gemini-ref/` was "strictly better" than the MiMo transcript. **That is too strong.** On `websockets…/03-conectando-o-front-end/1000-refactor-abstraindo-no-hook-usewebsockets`, the same passage reads:
  - MiMo (standard): `Record<string, any>` — fluent, confident, ×3
  - Gemini (reference): `record string N` — garbled; **0** occurrences of `any` *or* `unknown`
  ⭐ **The two transcripts are corrupted in DIFFERENT PLACES.** The reference is better *only for the specific term it was audited on*; it is not globally clean. Worse, `→ "N"` is the documented **Groq** signature, not Gemini's — so the ref tree's own provenance is not certain. ⛔ Never treat "read the reference instead" as a fix for a lesson; treat it as swapping one partial corruption for another, and reason from surrounding context for any term that matters.
- **Packs run 1.5–1.75× over the ~2k cap** (~2,900–3,500 tokens) once a course is actually read in full. course.md's remedy is "split **by module cluster**" — which is undefined for a single-module course, so the cap is being exceeded deliberately there rather than collapsing named distinctions. Flagged as advisory for 5.4, not silently passed.
- ⚠️ **Agents under-report `tokens_estimate`** (one claimed 2,250 on a ~3,500-token pack). The frontmatter number is a *claim*; `scripts/pack_coverage.py` now recomputes it.

### 🔧 New tool — `scripts/pack_coverage.py` (this is the 5.3 answer)

Content-based coverage reconciliation, replacing the counting artifact that blessed the 19. Matches every lesson transcript to a line in its pack's `## Curriculum` spine by title, and checks frontmatter keys, a plausible `compiled` date (`PROJECT_EPOCH` catches the invented `2024-07-30`), required sections, truncation, diacritics, and the token budget.

```bash
.venv/bin/python scripts/pack_coverage.py output/jstack-lives                          # audit the tree
.venv/bin/python scripts/pack_coverage.py output/jstack-lives --pack-glob '*.pack.v2*.md' --expect-today
```

⭐ **Validated against the known-bad corpus before being trusted**: it fails all 19 defective packs and measures them at **34% mean coverage** (median 42%, seven at 0% for having no `## Curriculum` at all) — independently reproducing the documented "22–66%, median 37%" by a different method. ⛔ **Its own limit, printed in its output: a matched curriculum line proves a lesson was not silently dropped — it does NOT prove the body was compressed faithfully.** A consistency gate is not a correctness gate; fidelity is still human review at 5.4.

## Architecture — three stages, two processing engines

**Stage 0 — authenticated acquisition (Python + exported browser session).** Platform adapters under `downloaders/` enumerate legitimately accessible curricula and write transcription-ready `.m4a` plus resumable manifests under `input/`. JStack has its mature adapter; DesignBoost and Skool share `_shared.py` for Netscape-cookie parsing, redacted logging, quiet signed-URL downloads, DRM refusal, atomic manifests, and audio/video modes. Login is never automated, and working artifacts remain gitignored.

**Stage 1 — transcription (Python + Gemini API · bulk, cheap).** `main.py` recurses `input/`, extracts audio (ffmpeg subprocess), transcribes each lesson with `gemini-2.5-flash` (thinking off, `google-genai` SDK + Files API) — or **Groq Whisper via `--provider groq`** (see Key decisions) — and writes `output/<course>/<module>/<lesson>.transcript.txt` — **mirroring the input folder tree**. The transcript is the deliverable / hand-off.

**Stage 2 — compilation (Claude Code agent · subscription, NO API).** The `course-module-compiler` agent (`agent.md` → symlinked into `.claude/agents/`) compiles **one pack per module** from that module's transcripts, via the `knowledge-compiler` skill's `course.md`. Defaults to **Sonnet**; returns `ESCALATE_TO_OPUS: <reason>` when a module is dense → the orchestrator re-spawns it on Opus. Packs are **staged** as `<module>/<slug>.pack.md`; a human promotes them to `knowledge/<domain>/courses/`.

## Key decisions
- **Granularity starts at source modules, then follows the reusable unit.** The initial 2026-06-19 rule was one pack per numbered leaf module; the Motion Boost re-cut on 2026-06-25 proved that section/tool/discipline clusters can be the better library shape. Never default to per-lesson or whole-course monoliths; reconcile every transcript against the chosen clusters.
- **Gemini = transcription only.** Compilation moved to the Claude agent (judgment + Sonnet/Opus choice, on the subscription). The old Gemini-formatting path is kept as legacy `--gemini-format`.
- **Stage 1 is provider-pluggable (2026-07-02; default changed 2026-08-04).** `--provider {openrouter,gemini,groq}`; **`openrouter`/`xiaomi/mimo-v2.5` is now the default** — see the 2026-08-04 State entry for the build, the two batch-scale bugs, and the standing jargon-fidelity caveat that keeps `gemini` the right call for code-dense courses. `groq` = Whisper `whisper-large-v3-turbo` over the OpenAI-compatible `/audio/transcriptions` endpoint — a purpose-built STT that's cheaper/faster and sidesteps both LLM failure modes (no 64k output ceiling, no phrase-repetition loops). Stage 2 is unaffected. See the 2026-07-02 State entry for the Zen dead-end + the live Groq verification and the `any`→"N" tradeoff.
- **Transcript is the default output**; `--gemini-format` is opt-in legacy.
- **`.venv` for BOTH test and production** (user rule). `.venv/` and `.env` are gitignored.
- **Download (Stage 0)** is handled by the [`course-downloader`](../../skills/course-downloader) skill → a per-platform `downloaders/<platform>.py` (first real one: `downloaders/jstack.py`). Audio-only `.m4a` is the default (in `AUDIO_EXTS`, so Stage 1 ingests it directly and skips its own ffmpeg extraction). The older sibling [`../one-click-video-downloader`](../one-click-video-downloader) (Cat Catch–based) stays for one-off single videos.

## Gotchas / implementation notes
- **ffmpeg via subprocess**, not moviepy (moviepy 1.0.3 is rough on Py 3.12; system ffmpeg is present). Audio normalized to **mono 16 kHz 64k MP3** to keep Files-API uploads small.
- **Files API is required** for course audio (inline request cap is ~20 MB). Uploaded files are deleted after each run (and auto-expire ~48h).
- **Date-hallucination fix:** the formatting prompt injects today's date — otherwise the model invents a `compiled:` date (it guessed `2023-11-20` on the first run). The Stage-2 agent carries the same "never invent a date" rule.
- **Idempotency:** re-runs skip a lesson whose final artifact already exists (`--overwrite` forces). An existing transcript is reused unless `--retranscribe` → cheap re-formatting only.
- **Batch resilience:** a bad file is caught + reported; the batch keeps going.
- **Model/key:** `gemini-2.5-flash`, thinking off — same provider/pattern as the PsiAtiva gender workflow. Single Google AI key in `.env`.

## Hardening from the first full course run (Motion Boost, 80 lessons, 2026-06-23)
The simple validation lesson hid four bugs that only a real nested course surfaced — all fixed:
- **Accented filenames, two separate breaks.** (1) Under an ASCII `C` locale (cron/bare shell/background), ffmpeg/subprocess on `módulo`/`composição` raises `'ascii' codec can't encode`. Fixed by a UTF-8 re-exec guard at the top of `main.py` (`-X utf8`). (2) The Gemini Files API sends the upload filename in an **ASCII-only HTTP header** (httpx `_normalize_header_value`), so even under UTF-8 the accented *temp audio* name raises `UnicodeEncodeError` on upload. Fixed by `_ascii_name()` in `pipeline.py` — the uploaded temp `.norm.mp3` is transliterated; the output transcript keeps the accented lesson name.
- **Free-tier daily cap = 20 `generate_content`/day.** Hit it at ~18 lessons. `gemini_client.generate_with_retry` now sleeps+retries per-minute 429s but raises `DailyQuotaExhausted` on a `PerDay` cap so the batch stops cleanly (resumable) instead of churning ffmpeg on doomed calls. **Resolution: user enabled billing → Tier 1**, then the rest flew through.
- **Frozen call hangs forever.** Machine slept mid-run → the in-flight HTTP call never returned → silent 11h stall at 25/80. Fixed with a **10-min per-request timeout** (`config.REQUEST_TIMEOUT_MS` → `HttpOptions(timeout=)`); a frozen call now errors and the batch continues/resumes.
- **Long-audio repetition loops.** 38-46 min `rive` lessons made the model loop one phrase (worst: **1011×**) until it burned all 64k output tokens (`MAX_TOKENS`). Fixed with **audio chunking**: files > `AUDIO_CHUNK_THRESHOLD_SEC` (15 min) are split into `AUDIO_CHUNK_LENGTH_SEC` (~10 min) segments (`audio.split_audio`), each transcribed and concatenated (`pipeline._transcribe_audio`). The 3 looped files re-ran to 5.9-7.5k sane words, 0 repeats.

## State (2026-06-23)
- ✅ **Stage 1 DONE on a real full course.** Motion Boost: **80/80 lessons, 12 module folders, 3 sections (conceito-e-teoria · after-effects · rive), 17.35h, ~158.6k words** — all transcribed clean (0 errors, 0 `MAX_TOKENS`) on Tier 1. Note the **3-level nesting** (course → section → module → lesson); Stage-2 "module" = the numbered leaf folders.
- ⚠️ **Footgun:** structure-mirroring is relative to the *target*. Run `main.py input/` (root = `input/`) for the full tree. An isolated batch is safe only when roots are paired explicitly (`main.py input/jstack-projects -o output/jstack-projects`); pointing at a subfolder while leaving the default output flattens it.
- ✅ **Stage 2 DONE — 12/12 module packs compiled & staged** (2026-06-23), all on **Sonnet** (none needed Opus, even the 17-lesson `cards-para-web` at ~1950 tok). Each is `<module>/<slug>.pack.md`, dated correctly, English structure + PT content, under the 2k cap, nothing leaked into `knowledge/`. Language convention chosen by user: **English skeleton, PT content** (cross-lingual).
- ✅ **Accent caveat RESOLVED.** First batch stripped PT diacritics on ~10 packs (`cards-para-web`/`lottie` fully ASCII) — a cross-lingual artifact of reasoning in English. Root-caused → `agent.md` now mandates "preserve diacritics EXACTLY" (English structure / PT content with full accents). All 10 regenerated in batches of 4; verified all 12 now carry proper accents (non-ASCII counts 54–195).
- 💡 **Batch lessons:** (1) spawning ~11 `course-module-compiler` agents at once hit a **session limit** mid-run — but agents that had already `Write`-n their pack kept it (disk is ground truth; reports said "limit" yet 11/12 packs were on disk). **Batches of ≤4 are safe; 11 is not.** (2) An agent flagged module-06 lessons 05/06 as "identical transcripts" — **false alarm** (distinct hashes, 40 vs 45 min source videos, different openings); they're distinct lessons sharing a project theme.
- ✅ **PROMOTED, then RE-CUT to section level (2026-06-25).** First promoted as 12 module packs (06-23); the user judged them too granular vs the `ux-unicornio-*` by-discipline shape, so re-cut to **4 section packs**: `motion-boost-{conceito-e-teoria, after-effects-fundamentos, after-effects-projetos-web, rive}.md` (After Effects split in two — 6 modules / 4 projects won't compress to one). The 12 old library files were deleted; `courses/` now holds 7 `ux-unicornio-*` + 4 `motion-boost-*`. Also distilled **4 reusable `design/guides/`** from this course (rive-interactive-animation · lottie-for-web · web-motion-delivery-decision · animating-ui-cards-for-web — filling the library's zero Rive/Lottie coverage) and fixed 2 dangling refs the rename created. Uncommitted. Two lessons: **(a) granularity = the source's reusable unit, not reflexively per-module** (section/tool here, like ux-unicornio's discipline); **(b) when the parallel `course-module-compiler` fan-out hits the account session limit, compile in the main thread from the already-approved module packs** (atomic Writes, resumable) instead of waiting for the reset. **Pipeline exercised end-to-end: 80 videos → 80 transcripts → 4 section packs + 4 guides.**

- ✅ **`formacao-full-stack` (JStack) DOWNLOADED — Stage 0, 2026-06-27.** 372 audio `.m4a` (4 sections setup/nodejs/react/react-native · 44 leaf modules) + 1 meta description, 0 failed, **4.3 GB**, in `input/formacao-full-stack/`. Ready for Stage 1 (note: ~12× typescript → needs the **paid Gemini tier**; several lessons are 27–32 min → chunking exercised). **Gotcha fixed:** `downloaders/jstack.py` hardcoded the typescript Bunny pull zone (library 627567 → `vz-ecd6b81a-333`); full-stack is library **349046 → `vz-968de784-70c`**, so every video URL 404'd (manifest showed a deceptive `done:1` = the videoless meta lesson). Now resolves the `vz-<hash>` host **per video** from `data.external.libraryId` (seeded map + Bunny embed-page auto-discovery for unknown libraries), plus `--force-ipv4`/`--socket-timeout`/retries in `download_ytdlp` (this WSL box has a **dead IPv6 stack + flaky DNS proxy** that stalled reads). Mirrored into `script-template.py`; lesson in `course-downloader/platform-recon.md` §4.

- ✅ **`formacao-full-stack` Stage 1 DONE — 2026-06-28.** **372/372 transcripts** under `output/formacao-full-stack/` (input tree mirrored). 3 transient errors on the first pass (DNS "Temporary failure in name resolution", SSL UNEXPECTED_EOF, Gemini 500 INTERNAL) → resolved by a plain re-run (idempotent, skips done) → 369→372, 0 errors. **False alarm:** "missing lesson NN" flags (nodejs 05/11/16, react-native 12) are **source numbering gaps** (JStack skips numbers), not data loss — verified counts match the dry-run. The long 27–32 min lessons exercised the >15-min audio chunker without repetition loops.

- ✅ **`formacao-full-stack` Stage 2 DONE — 19 packs compiled & STAGED, 2026-06-28.** All `output/formacao-full-stack/_packs/<slug>.pack.md`, all on **Sonnet** (0 escalated to Opus), English skeleton + PT content with diacritics preserved (non-ASCII 200–225/pack), `compiled: 2026-06-28`, ~1.6–2.0k tok (two small RN sub-packs at 0.95k/1.45k). **Coverage reconciled: 372/372 lessons, 0 orphans, 0 double-coverage** (table in session). **Re-cut by discipline like Motion Boost, not per-module:** big sections split into `-a/-b` (RN fundamentos-setup, RN ui-estilização, RN interação) and the 101-lesson MyContacts project cut into 6 (`-1`, `-2a/2b/2c`, `-3a/3b`). **Spawned `course-module-compiler` in batches of ≤4** (the safe ceiling); the account **session limit** stalled the last cluster twice (#13 RN-interação) — re-spawned after the daily reset, no rework lost (atomic Writes = disk is ground truth). **PROMOTED to `knowledge/coding/courses/` 2026-06-29** (19 files, `jstack-fullstack-*` slugs kept, matching `motion-boost-*`/`ux-unicornio-*`) after a hygiene pass: normalized `tokens_estimate` format + bumped react-estilização 1650→1900 (its body matched the 1850–1950 cohort); fixed **dangling See-also refs** in 8 packs — compiler agents had predicted sibling slugs that changed when clusters split (`react-mycontacts-2/3` → 2a/2b/2c + 3a/3b; `rn-interacao-a-botoes-eventos-forms` → the real `-eventos-formularios`) — and stripped 4 `Promotion target sugerido`/`(agente irmão)` scaffolding leaks. Staging ↔ library verified identical, **zero danglers (bare + full)**. **Also distilled 5 reusable `knowledge/coding/guides/`** (react-forms-controlled-inputs-and-validation · frontend-service-layer-and-api-errors · react-performance-and-re-renders · react-native-cli-vs-expo-decision · express-postgres-rest-api), English prose + `[[wikilink]]` cross-refs — filled the coding library's near-zero guide coverage (1 → 6). Uncommitted. **Lesson: after any cluster SPLIT, re-scan ALL packs for danglers with a regex that catches BARE slugs too (`jstack-fullstack-[a-z0-9-]+`), not just full `knowledge/coding/courses/…md` paths — the path-only grep silently missed the bare-slug See-also refs and they got promoted before a second scan caught them.**

- ✅ **`formacao-full-stack` guides + principles EXTENDED — 2026-06-29 (cont.).** Fixed the TypeScript course's **2 cross-referencing `.pack.md` dangling refs** (`formacao-typescript-…-parte-1` ↔ `parte-2` pointed at each other by bare `.pack.md` filename → corrected to full `knowledge/coding/courses/…md` paths; also repointed `02-objetos-e-arrays` See-also at the real promoted parte files). Distilled **8 more `knowledge/coding/guides/`** (Tiers 1–3): `typescript-config-and-type-system` · `react-component-architecture` · `css-animations-enter-exit-without-a-library` · `styled-components-theming-and-dark-mode` · `react-router-v5-to-v6-migration` · `node-runtime-and-the-event-loop` · `react-native-ui-styling-and-lists` · `docker-local-dev-databases` → coding guides now **13** (was 6, incl. cpanel). Updated `coding-principles.md` Source map to index all 13 + added 5 Apply-line guide pointers (`tokens_estimate` 1800→1850). **Re-verified every `[[wikilink]]` + full-path course/principles ref across all 13 guides resolves — 0 broken (bare + full).** Course pipeline now fully distilled end-to-end; uncommitted (user commits later).

- ✅ **Alternative transcription provider added — Groq Whisper, 2026-07-02.** New `--provider {gemini,groq}` seam (**gemini still default**); `groq` POSTs the normalized audio to Groq's OpenAI-compatible `/audio/transcriptions` (`whisper-large-v3-turbo`) via the existing `httpx` dep — no Files-API upload/poll/delete dance, cheaper (~$0.04/hr audio), and immune to the two LLM failure modes we engineered around (64k output ceiling + phrase-repetition). New `course_to_markdown/groq_client.py` (retry/backoff on 429/5xx honoring `Retry-After`, ISO-639-1 language hint, per-file multipart); `config.get_groq_api_key()` + `DEFAULT_PROVIDER`/`DEFAULT_GROQ_MODEL`; a provider switch in `pipeline._transcribe_one`; `--provider`/`--model` (model default tracks provider) in `main.py`. **Chunking, idempotency, and accent handling reused unchanged** (chunking also keeps files under Groq's 25 MB free-tier per-file cap). **Verified live:** `--provider groq` on one lesson with the **Gemini key deliberately broken** → real PT transcript, diacritics intact (á ã ç é ê í ó), exit 0 — proves it's Groq, not a silent Gemini fallback; the no-key guard errors cleanly (`exit 1`, writes nothing); `--gemini-format` refuses a non-gemini provider (`exit 2`).
    - **Tradeoff to know:** Whisper turbo mis-renders English technical terms embedded in PT speech — on the TS course it wrote the type **`any` as "N"** ("o tipo N", "um N.ts"). Fine for prose-heavy courses; for code-dense ones Gemini reads the English keywords better, though Stage-2 Claude compilation usually repairs it from context. Choose per course.
    - **Zen (opencode) was evaluated and REJECTED.** The user's new Zen key is a **text-only** coding gateway: only Gemini models accept audio, and Zen's Gemini integration is broken (HTTP 400 `Missing key ['contents']` on **every** call — text *and* audio, OpenAI *and* native formats), while every working Zen model (Claude/GPT/DeepSeek/…) is text-only. So Zen **cannot** do Stage-1 transcription at all — Groq is the real cheaper-than-Gemini STT path. (Cloudflare also 1010-blocks urllib's default UA; moot once Zen was ruled out.)
    - **Free-tier note + concrete limits:** Groq rate-limits per **organization**; for our model `whisper-large-v3-turbo` the binding constraint is **audio-seconds, not requests** — **20 RPM · 2,000 RPD · 7,200 ASH (≈2 h audio/hour) · 28,800 ASD (≈8 h audio/day)** (published base numbers; confirm your org's exact values at https://console.groq.com/settings/limits — full table + headers mirrored at `knowledge/sources/groq-rate-limits.md`). So a big course exceeds the **daily audio budget**: Motion Boost's 17.35 h ≈ 3 days on free tier, JStack full-stack far more — plan multi-day runs or use Groq's paid Developer plan. 429s carry `retry-after` (seconds); `groq_client` honors it. **Daily/hourly-cap clean stop (hardened 2026-07-02):** the Groq path now mirrors Gemini's `DailyQuotaExhausted`. `groq_client._rate_limit_stops_run` reads a 429's body — "per day" (RPD/ASD) or "per hour" (ASH) → **stop**; "per minute" (RPM) → **retry**; unparseable → stop only if `retry-after` > 300 s (`STOP_IF_RETRY_AFTER_OVER_SEC`) — and raises `groq_client.DailyQuotaExhausted`, which `pipeline.process_file` re-raises and `run()` catches to **break the batch cleanly** (resumable; finished transcripts skipped). Per-minute 429s still back off in-loop; the HTTP-200 happy path is untouched. A big free-tier course now stops at the daily audio cap and resumes next run instead of erroring every remaining file. Verified by unit test (8-case detection matrix + full stop-path wiring; no live quota burned). Key at https://console.groq.com/keys → `GROQ_API_KEY` in `.env` (gitignored). Uncommitted (user commits later).

- ✅ **JStack downloader GENERALIZED to the whole catalog — Stage 0, 2026-07-17.** `downloaders/jstack.py` was already per-course generic (it took any course URL); what was hardcoded was the **`trainings` path segment**. It now handles all **three** JStack content types via the SPA's own map (`LIVE`→lives, `PROJECT`→projects, `TRAINING`→trainings), with `--list` (whole catalog) and `--all {lives,projects,trainings,all}` (sequential batch, `--limit N`, resumable, one bad course reported without killing the run). **No per-course script was ever needed.**
    - **Catalog (probed 2026-07-17):** **69 lives + 3 projects + 2 trainings = 74**, all unlocked on this account; ~1,243 videos total (≈3× formacao-full-stack). Lives are the "mini-cursos" (numbered #0–#68; Back-end 35 · Front-end 28 · Full-stack 4 · Infra/DevOps 2). Projects = foodiary/fincheck/waiterapp.
    - **Endpoints:** `GET /contents/{lives,projects,trainings}` list; `GET /contents/<seg>/<slug>` detail. **`/search` is `POST /contents/search`** (a GET 404s) and merely groups the three types — the three list endpoints are the real enumeration source.
    - **Shape differs PER TYPE — detect, don't assume.** trainings/projects: detail is a SECTION/MODULE skeleton with **no** video data → each leaf module must be fetched (`/contents/<seg>/<slug>/modules/<path>`). **lives: the detail response carries the WHOLE MODULE→VIDEO tree inline** and their `.../modules/` endpoint **404s**. `enumerate_course()` therefore uses inline VIDEO nodes when present and only falls back to the per-module fetch. (Assuming the training shape for lives cost a round of 404s; the SPA bundle's `getFullModule` *implies* the modules path works for all types — it does not.)
    - **`/contents/lives` is CURSOR-PAGINATED** (`nextPage`, base64 DynamoDB key, 16/page). An un-paged GET returns 16 of 69 and looks successful. The other two endpoints aren't paginated.
    - **Lives library = 300830 → `vz-e9bd0866-1a0`** (seeded in `BUNNY_PULL_ZONES`; all 68 Bunny lives share it).
    - **YouTube outlier:** live **#66 `dominando-os-react-hooks`** has `nodes: []`; its recording is at `metadata.youtubeURL` (mirrored under `metadata.protected`). Without `_youtube_lesson()` it enumerates to **zero lessons and writes a clean-looking manifest** — the same silent-success trap as the wrong-pull-zone bug. An empty tree with no youtubeURL is now *reported*, never marked done. **Gotcha found in test:** the YouTube lesson first collided with the course meta lesson (both `00-<slug>` → one shared `lesson_id()` → `--resume` could mark the blurb done and skip the real video); the YouTube lesson is now `order=1`.
    - **Layout:** lives → `input/jstack-lives/<slug>/`, projects → `input/jstack-projects/<slug>/`, **trainings stay flat** at `input/<slug>/` (legacy; the two `formacao-*` are already there). Rationale: `main.py input/` would otherwise sweep all 69 lives plus the formações into one Stage-1 run. `--course-slug`/`--out` are rejected with `--all` (they name one folder, not N).
    - **Smoke-verified end-to-end (user chose 2–3):** `rbac-criando-um-sistema-de-controle-de-acesso-gerenciavel` → **7/7 done, 0 failed, 91 MB** real `.m4a` (proves the inline path + new pull zone); `dominando-os-react-hooks` → **2/2 done** (proves the YouTube path); `formacao-typescript` dry-run **unregressed** (31 lessons, accents intact, still flat in `input/`). Batch driver exercised with `--all lives --limit 3 --dry-run`. **Not yet downloaded: the other 67 lives + 3 projects.**
    - ⚠️ **`order: 999` is an "unset" sentinel, and the `1000-*` folder wart is PRE-EXISTING — left alone deliberately.** 63 live modules + 92 live videos carry it, so `f"{order+1:02d}"` yields `1000-conteúdo`; one live (`preparando-apps-react-para-producao`) has *four* modules all at 999, so the field is meaningless there. **This already shipped in the promoted `formacao-full-stack` tree** (`01-setup/1000-ambiente-de-desenvolvimento`, `02-nodejs/1000-nodejs-com-express`, …) — all 19 packs were compiled from it. Renumbering (e.g. index-based fallback) would rewrite every `lesson_id()` → **`--resume` would re-download whole courses** and new trees would diverge from the promoted ones. Decide deliberately before "fixing" it; also note **39/69 lives wrap everything in a single catch-all module named `conteudo`**, a useless folder level that flattening would remove. Uncommitted.

- ✅ **DesignBoost + Skool Stage 0 adapters shipped — 2026-07-28.** Added `downloaders/designboost.py`, `downloaders/skool.py`, and the shared `_shared.py` safety/resume core. DesignBoost uses the URL-decoded `design-boost.auth_token` from the member-session export against `api.designboost.com.br/student`; it covers `courses`, `workshops`, `single-classes`, and `ai-classes` (AI catalog entries resolve through `/courses/<id>`). An authenticated live dry-run on course `32` resolved **12/12 Vimeo lessons** over two modules. Skool uses the full exported cookie jar plus a normal browser user-agent, parses authenticated `__NEXT_DATA__`, resolves external video links or the page's signed native `stream.video.skool.com` HLS, extracts TipTap description/resource links, and probes commercial HLS DRM markers before download. The supplied `Equação de Valor` URL resolved **1/1 native lesson**, no DRM. No Playwright dependency was needed: the original 403 was a plain-client fingerprint mismatch, not an invalid session.
    - **Security invariants:** signed queries are never persisted in manifests; shared yt-dlp runs quiet and its captured errors pass through redaction; bearer/cookie headers and token/signature query parameters are masked; sensitive resource queries are stripped; commercial DRM is marked/skipped, never decrypted.
    - **Verification:** `compileall`, **13/13 offline safety/contract tests**, and post-hardening authenticated dry-runs on both platforms pass. Cookie/media/transcript trees are covered by `/input/*` and `/output/*` ignores.
    - **Scope boundary:** adapter support does not authorize bulk acquisition of every visible DesignBoost/Skool course. The current user request explicitly calls for finishing the existing JStack backlog; choose new-platform download scope separately.

- ⏸ **JStack catalog completion checkpoint — 2026-07-28.** The new `input/cookies-jstack.txt` export was live-verified against the complete **69 lives + 3 projects + 2 trainings = 74** catalog. Stage 0 remained active at item 20/69 with a 467-media / 21-manifest snapshot, zero failed statuses, and one active downloader partial. Gemini Stage 1 was stopped at the user's request and a process check confirmed no `main.py` workers remain. Projects have 182/182 non-empty transcript files, but one Fincheck lesson still fails the repetition audit (the five-minute repair returned `STOP` while repeating one 8-word sequence 1,051 times); its three-minute repair was interrupted before replacement. Lives had 325 transcripts at the snapshot; Multi-tenant stopped at 6/28 and Fastify at 22/26, while React Router 40/40, Serverless 36/36, DynamoDB/Algolia 21/21, and the other counts are recorded in `TASKS.md`. Stage 1 now rejects non-`STOP` finishes plus exact 8-word sequences repeated 50+ times; 13/13 offline tests pass. **Do not restart Gemini until the user explicitly resumes it.** Then repair Fincheck first, run the quality scan, and only afterward reconcile projects/fan out lives. `scripts/transcribe_batches.py` remains restricted to a clean Stage 0 exit because a manifest cannot prove that an upstream downloader will not discover a brand-new lesson.

- ✅ **Fincheck repetition-loop lesson REPAIRED + projects quality-complete — 2026-08-04, via OpenRouter/MiMo, NOT Gemini.** Phase 3.3 closes: **182 media / 182 transcripts, quality scan 0 flags**. The lesson that survived two Gemini repair attempts (`02-front-end/03-dashboard/04-implementando-dropdown-de-sair-da-conta`, 54.4 min) went from **11,928 words with an 8-word phrase ×1,051** to **10,444 words / ×4 clean**, diacritics intact (1,924 non-ASCII). ⚠️ It is the corpus's **only non-Gemini transcript**.
    - **Why not Gemini:** an A/B on that exact lesson showed the failure is the model, not the chunking — it looped even at 5-min chunks, and the loop starts at 78% right after the instructor hesitates (*"a gente tem, cadê, cadê, cadê?"*). **`gemini-2.5-flash-lite` carries the identical 65,535 output ceiling**, so "switch to the cheaper Gemini" would have saved money and left the lesson broken. Stage 1 remained paused throughout; no Gemini call was made.
    - **A/B result (n=1, then widened):** `xiaomi/mimo-v2.5` **10,444 w / 8-gram ×4 / $0.0129 / 294s** · `mistralai/voxtral-small-24b-2507` **10,786 w / ×4 / $0.3307 / 98s** · gemini-2.5-flash ×1,051 REJECT. **MiMo ≈ $0.0142/h — the cheapest option surveyed, ~11× under the flash default and ~2.8× under Groq/flash-lite** — and both challengers rendered English jargon faithfully (`Radix`, `Material UI`, `Tailwind`), so Whisper's `any`→"N" defect did **not** reproduce. Voxtral is the speed play (3×) at 25× the cost; it did catch the proper noun `Fincheck` where MiMo wrote "front-end".
    - ⛔ **No `openrouter` provider was built** — deliberately. n=1 is too thin to change a production pipeline; widen the A/B first. Probe lives at `scratchpad/ab_openrouter.py` (throwaway, cached chunks + per-chunk incremental saves so a re-run never re-pays).
    - 🔴 **WIDENING TO n=4 OVERTURNED THE GENERAL RECOMMENDATION — MiMo must NOT become the Stage-1 default.** On `formacao-typescript/…/19-a-opção-strict-do-tsconfig` it transcribes the type **`unknown` as `any`, 7/7 occurrences** (Gemini `unknown`×7→MiMo×**0**, `any`×10→×**16**), emitting *"any permite que você acesse qualquer coisa e any não permite"* — inverted meaning. Gemini got that passage right. **This is worse than Groq's `any`→"N" because MiMo's errors are FLUENT: they pass loop detection, `finish_reason`, word count, diacritics and wpm, so Stage 2 would compress a WRONG rule into the library.** ⛔ **Loop-freedom is not a proxy for fidelity.** Net effect: the widened A/B **confirms the existing per-course rule in `CLAUDE.md`** — code/jargon-dense course → Gemini. MiMo joins Groq in the cheap-but-jargon-lossy bucket.
    - ✅ **The installed Fincheck repair was re-verified against this defect and is SAFE** — React/UI vocabulary only, no `unknown`-class terms; every high-frequency term tracks Gemini's pre-loop portion (`radix` 31→38, `dropdown` 100→91, `menu` 65→67), and MiMo recovered the final **22% Gemini lost to the loop**. **MiMo's correct niche = repairing a Gemini loop failure on non-TS/UI content, not general transcription.**
    - Other widened runs, all clean (`finish=stop`, no loops): `05-dropdown-filtro` 3,193 w/×2/$0.0034 · `02-slider-de-meses` 9,739 w/×2/$0.0119 · `19-strict-tsconfig` 2,519 w/×1/$0.0019. ⚠️ Jargon checks: build patterns as `rf"\b{term}\b"` — a `\\b` inside an r-string silently returns 0 for every term and reads as a pass. Tool: `scratchpad/jargon_diff.py`.
    - **OpenRouter gotchas that cost real time:** ⛔ audio is hard-gated behind a **$0.50 account balance** (`402`, applies even to `:free` models and a 15-second clip) · ⛔ `voxtral` **400s on `temperature: 0` unless `top_p: 1` is also sent** (Mistral code 3054, surfaces as a generic "Provider returned error") · ⛔🔴 `nvidia/nemotron-3-nano-omni-*:free` advertises `audio` in `input_modalities` but **its provider silently drops it**, returning HTTP 200 + `finish_reason: stop` and a plausible short PT reply — **a catalogue modality flag is not proof a provider delivers it**; guard with a words-per-minute check (real lecture speech ~150–200 wpm, under 30 = the audio never arrived). ⛔ **Every DeepSeek model on OpenRouter is text-only** — `deepseek-v4-flash` cannot transcribe audio.
    - Scan script rebuilt at `scratchpad/quality_scan.py` (media↔transcript reconciliation + the 8-gram×50 loop guard + empty/orphan/`.part`/zero-byte checks); the `/tmp` original referenced by `TASKS.md` was cleared.

- ✅ **`openrouter` provider BUILT and made the Stage-1 default — 2026-08-04** (`course_to_markdown/openrouter_client.py`, `--provider {openrouter,gemini,groq}`). Base64-inline audio → `xiaomi/mimo-v2.5`. Done at the user's explicit instruction, *with the fidelity caveat above still standing*: the default optimizes cost and loop-immunity, **not** jargon fidelity. `scripts/jargon_audit.py` is mandatory for code-dense courses; `--provider gemini` remains the fidelity choice.
    - 🔴 **Two production bugs the A/B never surfaced, both found only at batch scale:**
      1. ⛔ **The chunk THRESHOLD straddled the provider's hard cap.** MiMo's providers reject audio >600s with a 400 (`Audio exceeds maximum allowed duration of 600s`), but `AUDIO_CHUNK_THRESHOLD_SEC` is 900 — so **every lesson between 10 and 15 minutes was sent whole and hard-failed**. Fixing the chunk *length* alone would not have helped; the threshold is the load-bearing half. Fix: `OPENROUTER_MAX_AUDIO_SEC = 540` clamps **both** threshold and length, provider-aware, in `_transcribe_audio`. Verified on the exact 700.4s lesson that failed → 2 chunks, 2,219 w @ 190 wpm, clean.
      2. ⛔ **No `max_tokens` on a reasoning model.** MiMo could spend the provider's default budget entirely on reasoning and return `finish_reason="length"` with **zero content — billed in full** (22 lessons). Fix: explicit `max_tokens` + `"reasoning": {"enabled": False}`. ⚠️ `exclude: true` is **not** the fix — it still generates and bills the reasoning. Transcription needs no chain-of-thought.
      - 💰 These two burned the run rate from ~$0.0024 to ~$0.013/lesson before being caught. **A failed generation still bills.**
    - ⭐🔴 **THE BIG ONE: an OpenRouter model name is not one model — pin the backend.** Six providers serve `xiaomi/mimo-v2.5` with different quantization, output ceilings, audio support and price, and **auto-routing sent every single observed request to the worst of them (DeepInfra)**. One identical 540s chunk: **Xiaomi fp8 → clean 1,771 w, $0.00125** · **DeepInfra bf16 → LOOPED 8,003 w, $0.03338** (27× a clean chunk) · DeepInfra clean → $0.00667 (5.3× Xiaomi) · Novita/Venice **reject audio** (404 "no allowed providers" / 429). This single fact explained four symptoms we had been treating as separate bugs: the `finish_reason="length"` failures (DeepInfra's **16,384** ceiling sits right at a working `max_tokens`), the repetition loops, the `Audio exceeds 600s` rejections, and the 5× cost overrun. Fix: `config.OPENROUTER_PROVIDERS=["Xiaomi"]` sent as `provider:{only:[...], allow_fallbacks:False}` — pin **hard**, since a silent fallback restores both the price and the loop. **Measured effect: $0.0117 → $0.0032/lesson (3.7×), turning "cannot finish, strands at ~lesson 700" into "finishes with margin."**
    - ⚠️ **The loop is stochastic, and neither chunk size nor quantization fully explains it.** Byte-identical requests at `temperature: 0` looped **~1 in 6** (8-word cycle × 7,600); halving chunks 540s→300s looped exactly the same, so ⛔ **the old "retry with shorter chunk env values" advice does not work for this failure**. Pinning to fp8 cut the rate hard (~13% → ~1 in 60) but did **not** eliminate it — one chunk looped on all 4 attempts — so bf16 was at most part of the cause. Retrying is the real remedy (`TruncatedGeneration` now retries alongside `AudioNotReceived`); measured 75% recovery (9 of 12).
    - ⚠️ **MiMo silently drops the audio on ~7% of requests** (4/57 measured) — HTTP 200, `finish_reason: stop`, fluent *"I don't see an audio file attached"*. Same class as `nemotron-omni:free` but **intermittent**, so it reads as random lesson failures. The wpm floor is the only detector; `transcribe_file` now retries it like a 429. Treat a few `did not receive the audio` errors as normal residue, not broken media.
    - 📉 **`jargon_audit.py`'s orphan-pair heuristic is base-rate noise at corpus scale** — on 392 jstack-lives transcripts it flagged **23.5% of MiMo output vs 24.6% of Gemini's**: indistinguishable. A lesson that simply never says `let` trips it; one Gemini transcript even shows the `any×7 / unknown×0` signature of the known defect. ⭐ **Only the `--reference` diff discriminates** (it still flags the known-bad pair on all three detectors, incl. `TERM LOST`). Spot-check n=3 on jargon-dense jstack-lives lessons: **no substitution**, and MiMo beat Gemini on `registry.getInstance` (Gemini split it). Plausible mechanism: MiMo collapses a *rare* term into its common near-synonym (`unknown`→`any`); proper nouns (DynamoDB, Fastify, Cognito) have no neighbour to collapse into. **Spot check, not a clearance.**
- ✅ **`scripts/reconcile.py` — TASKS 3.6+3.7 as a repeatable gate (2026-08-04).** Media↔transcript, manifest↔media, `.part`/zero-byte, plus two checks earned this session. 🔴 **A clean manifest proves nothing:** a killed downloader leaves `.part`/`.ytdl` and writes **no manifest entry at all**, so the course reads 100% `done` with the lesson simply absent (dynamodb lesson 11). The manifest records what the downloader *finished*, never what the curriculum *contains*. The **numbering-gap** advisory (05, 09 missing from 01..11) was vindicated on first use — those lessons were real and downloaded on the next pass.
- ✅ **Phase 3.4 lives media COMPLETE — 69/69 courses, 13 GB (2026-08-04)**, after the cookie was renewed. Catalog 69 lives / 22 previously local / 47 fetched; **zero local courses absent from the catalog** (no renames). 2 lessons failed on transient network errors (DNS, SSL EOF) and were retried. ⚠️ The Stage-1 fan-out then **correctly refused to start** on those 2 dirty manifests — do not bypass that validator with an ad-hoc launcher (a previous session did, and left duplicate workers writing the same transcript plus an orphaned `xargs` dispatching courses nobody was tracking).
- ✅ **Phase 3.5 COMPLETE — JStack Stage 0/1 fully transcribed, 2026-08-04. `jstack-lives` 1084/1084 across 69 courses, `reconcile.py` 0 needing attention;** corpus total 1,668 (lives 1084 · projects 182 · full-stack 372 · typescript 30), zero `.part`/`.ytdl`/zero-byte anywhere.
    - **Cost, measured over 527 lessons in one pass: $1.2511 = `$0.0024/lesson`** — *better* than the $0.0032 the pin was originally measured at, over a much larger sample. The Xiaomi pin holds at scale.
    - ⚠️ **The second retry pass cost `$0.022/lesson`, 9× the clean rate, for a 33% recovery** (5 of 15) — far below the 75% measured earlier. **A looping generation bills to the `max_tokens` ceiling, so retrying loops is the most expensive thing this pipeline does.** Budget a retry pass as ~10× a clean one, and stop after two.
    - 🔴 **Two loop populations, and only one responds to retrying.** The *stochastic* loop (~1-in-6, backend-dependent) recovers on retry — that is the documented cure. But **9 lessons looped on all 4 retries across two consecutive passes**: deterministic for this model+audio pair. ⛔ **Retrying those is pure burn.** The cure is a **provider switch** — the exact inverse of the Fincheck case, where MiMo rescued a lesson Gemini could not do. `--provider gemini` cleared all 9 in one sweep, **69/69 clean, exit 0**. Lesson: **when a lesson fails the same way twice, change provider; do not change chunk size and do not retry a third time.**
    - ⭐🔴 **A guard fired on its own arithmetic, not on a real fault — the wpm floor false-positived on a chunk sliver.** `ffmpeg -f segment -segment_time 540` emits a remainder chunk whenever a lesson runs just past a multiple of the chunk length; a 1622s lesson leaves a **2.0s tail**. MiMo correctly returned the single word spoken there (*"Beleza?"*), which scored **28.2 wpm against a floor of 30 — failing the entire 27-minute lesson**. It read as a provider fault for two full passes. Fix: `MIN_DURATION_FOR_WPM_SEC = 30.0` in `openrouter_client.py` exempts short chunks; the guard had **no power** below that anyway, since the ~8-word silent-drop apology it hunts scores 240 wpm on a 2s chunk. Recovered the lesson on MiMo immediately (5,275 w @ 195 wpm, 1,010 diacritics, 8-gram ×3). ⛔ **Any lesson whose duration lands just past a 540s multiple would have failed identically** — this was a latent corpus-wide defect, not one bad file. Tests 13 → **26/26** (one for the sliver, one asserting the exemption is not a hole).
    - **Provenance is now mixed — check it before blaming Stage 2.** The corpus is MiMo/OpenRouter except **1 Fincheck lesson** and the **9 lives lessons** listed in `TASKS.md` 3.5, which are Gemini. The Gemini pause was lifted 2026-08-04 with explicit user approval for that bounded sweep only; `openrouter` remains the default.
    - ✅ **`jargon_audit.py` RUN 2026-08-05 — see the Phase 5 State entry below for the result** (3 of 19 reference pairs lose a term; the `unknown`→`any` case reproduced verbatim, but it did not reach the library). ⛔ Bare-mode auditing is base-rate noise (measured 23.2% on the full 1,084); only `--reference` discriminates.

- 🔴 **Phase 5 (Stage 2 for the new corpus) SHIPPED ON A FORBIDDEN PATH — 102 packs staged, 19 must be redone. 2026-08-05.** `scripts/compile_lives_stage2.py` (untracked, **delete it — do not commit**) compiled 19 jstack-lives packs with `gemini-2.5-flash` **over the Gemini API**, the single thing Stage 2 must never do. The rule was never ambiguous; the violation arrived as a convenience script, which is how a hard rule actually gets broken. Four lessons worth keeping:
    - ⭐🔴 **A prompt-slicing compiler fails silently and *nothing downstream announces it*.** The script capped each lesson at 15k chars and then sliced the whole prompt at `[:60000]`. **All 19 courses overflowed it** — coverage **22–66%, median 37%**, across 272 lessons (worst: 25 lessons → 22%). Every pack still looked plausible, because a model given the first third of a course writes a confident pack about the first third of a course. ⛔ **Never build a Stage-2 path that concatenates a whole course into one prompt** — the `course-module-compiler` agent exists precisely because per-module compilation has no window to overflow.
    - ⭐🔴 **"Coverage reconciled, zero orphans" was a counting artifact, and it is the most dangerous kind of green check.** The reconciliation asserted that every transcript sits under a course that *has* a pack — never that its content reached the compiler. It passed at 100% while the compiler had read ~37%. **Reconcile on what the compiler ingested, not on folder membership** — the same family of error as [[feedback_predeploy_gate_cannot_be_the_artifact_deployed]] and [[feedback_http_200_is_not_liveness]]: the check answered a question adjacent to the one that mattered.
    - **Two known-and-fixed bugs came back because the script did not inherit the pipeline's config.** `max_output_tokens=3000` with **thinking left ON** → the budget went to reasoning and **6 packs truncated mid-sentence** (358–725 B vs an 8.9 KB median); `import datetime` present but **unused** → the model invented `compiled: 2024-07-30`, two years past, the same hallucination the 2026-06 fix cured in `transcribe.py`. A side script that re-implements a call the package already makes correctly will re-acquire every bug the package already paid for.
    - Also across all 102 staged packs: **28 lack the required `## How to apply`** (14 in the Gemini-19, 14 dated today) and 3 carry quoted-date drift. Untouched and trustworthy: the 19 `formacao-full-stack/_packs` (2026-06-28) and the older motion-boost/typescript packs — those went through the reviewed subagent flow and are already promoted.
- ✅ **Jargon audit finally run — 2026-08-05. The corruption is real at 16%, but it did not reach the library.** Built a purpose-built 19-lesson Gemini baseline at `output/jstack-lives-gemini-ref/` (`.logs/gemini-ref-sweep.log`, ok=19 failed=0) and diffed it against the MiMo corpus. **3 of 19 pairs lose a term.** The documented failure reproduced verbatim in `gerenciamento-de-estados-com-zustand/…/06-configurando-o-zustand-com-o-typescript`: Gemini *"ele tá como **unknown**, porque eu não passei nenhum generic"* vs MiMo *"ele tá como **any**"* — `unknown` ×8 → ×0, semantically inverted. The other two drop `callback` ×7 and `context` ×3.
    - ⚖️ **The feared outcome did not happen.** That pack propagates **no wrong `any`/`unknown` rule** — neither term survived compression, so the defect became an **omission, not an inversion**. Worth knowing before the next panic: compression can absorb a substitution as easily as it can amplify one. Do not assume propagation; check the pack.
    - ⛔ **Bare mode is confirmed dead at corpus scale:** 252 of 1,084 flagged = **23.2%**, matching the documented base rate almost exactly. Only `--reference` discriminates — the docs were right.
    - ⚠️ **`jargon_audit.py` wart:** the heuristics run *even in* `--reference` mode (lines 138-139), so `jargon_audit.py output/jstack-lives --reference output/jstack-lives-gemini-ref` buries the 19 real diffs under 252 noise flags and reads as if it found nothing useful. Filter on `TERM LOST`/`down N%`, or make `--reference` suppress the heuristics.

## Map
- `downloaders/` — Stage-0 adapters + shared authenticated/resumable core
- `main.py` — Stage-1 CLI · `course_to_markdown/` — package (`config`, `audio`, `gemini_client`, `prompts`, `transcribe`, `formatter`, `pipeline`)
- `agent.md` — Stage-2 agent (→ `.claude/agents/course-module-compiler.md`)
- `input/` (drop the course here) · `output/` (transcripts + staged packs) — both gitignored
- `TASKS.md` — active execution state · `ROADMAP.md` — deferred/v2+ scope
- Skill: [`../../skills/knowledge-compiler`](../../skills/knowledge-compiler) · Idea: `knowledge/ideas/course-videos-to-knowledge-markdown.md`

## 📚 Detailed history

⚠️ **This repository is PUBLIC, so the full internal history is deliberately NOT kept here.** This file carries the sanitized technical state only.

The complete record lives in the private `ai-synthesizer` workspace at `knowledge/projects/_memory/project_course_to_markdown.md` — session-by-session, including the parts that must not be published (hosting account details, client agreements, internal IDs). Folded there 2026-08-17.
